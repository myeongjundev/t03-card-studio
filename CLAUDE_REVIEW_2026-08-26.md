# ALTER / EGO — 리뷰 보고서 (2026-08-26)

리뷰어: Claude (시니어 프로덕트 디자이너 / 프런트엔드 엔지니어 관점)
범위: 로컬 `main` 작업트리 + 공개 페이지 https://myeongjundev.github.io/t03-card-studio/
**코드는 수정하지 않았다.**

---

## 0. 기준 대조 (요청 §1)

| 항목 | 프롬프트 설명 | 실제 |
| --- | --- | --- |
| 기준 커밋 | `6b1b4a4 feat: brighten the studio with sky and ivory` | **`f15f754 docs: refresh Claude portfolio review prompt`** — 한 커밋 앞서 있음 (문서만 변경, 코드 동일) |
| 브랜치 | `main` | `main` ✓ |
| 작업트리 | — | 깨끗함 (`git status` 출력 없음) ✓ |
| 스택 | React 19, Vite 8 | `react ^19.2.8`, `vite ^8.2.2` ✓ |
| 런타임 의존성 | `react`, `react-dom` | 정확히 그 둘뿐 ✓ |
| 단위 테스트 | 70개 | **70개 전부 통과** ✓ |
| E2E | Chromium 12개 | **12개 전부 통과** ✓ |

문서의 주장과 실제가 어긋나는 부분은 없었다. 커밋 해시만 한 칸 앞서 있고 그 커밋은 `CLAUDE_REVIEW_PROMPT.md` 단일 파일 변경이다.

---

# 발견 사항 (심각도 순)

---

### [P1] Scanner 방향키가 실제로 아무것도 움직이지 못한다

- **위치**: `src/components/TemporalScanner.jsx:136` (`onKeyUp={snap}`), `:97-106` (`handleKeyDown`)
- **문제**: 화살표 키 한 번의 이동량(0.025)보다 키를 뗄 때 실행되는 `snap()`의 흡인 범위가 훨씬 넓어서, 키를 누르고 떼는 순간 값이 원래 정지점으로 되돌아간다.
- **재현**:
  1. 첫 화면에서 Tab으로 `.scanner-frame`에 초점을 준다 (초기 progress = 1, 즉 2026).
  2. `ArrowLeft`를 **눌렀다 뗀다.** 8번 반복한다.
  3. TIME INDEX 판독값은 계속 `2026.00`이다. 화면도 그대로다.
  4. 키를 **누르고 있으면**(auto-repeat) `2017.60`까지 내려가지만, 떼는 순간 `2012.00`으로 스냅된다.
- **근거**: **실제 브라우저.** 로컬(`localhost:5173`)과 **공개 페이지 양쪽에서 동일하게 재현**했다.
  ```
  {"site":"PUBLIC","start":"2026.00","after8ArrowLeftTaps":"2026.00","moved":false,"afterHome":"2004.00"}
  ```
  코드상으로도 progress 0.975의 최근접 정지점은 1이므로 한 번의 키 입력은 구조적으로 항상 취소된다.
- **영향**: 키보드 사용자에게 이 제품의 **시그니처 인터랙션이 3버튼 라디오로 축소된다.** `Home`/`End`만 동작하고 `ArrowLeft`/`ArrowRight`는 완전히 무효다. `role="slider"`를 선언해 놓고 슬라이더로 동작하지 않는 것은 접근성 위반(WCAG 2.1.1)이며, README는 "포인터·터치·**방향키**로 2004·2012·2026을 탐색"이라고 명시적으로 광고하고 있다. 포트폴리오 심사자가 마우스를 놓고 키보드를 잡는 순간 가장 먼저 깨지는 지점이다.
- **제안**: `onKeyUp={snap}`을 제거하고, 스냅은 `onBlur`와 포인터 종료에만 남긴다. 키보드 조작은 `handleKeyDown`이 끝난 상태를 그대로 유지하게 둔다(중간 위치에 머무를 수 있어야 slider의 의미가 산다). 또는 화살표 한 번의 이동량을 0.5(정지점 단위)로 바꿔 3단계 이동으로 명시하되, 그러면 `role="slider"` 대신 `radiogroup` 의미가 맞다. 어느 쪽이든 편집 상태는 건드리지 않으므로 "Hero 탐색과 편집 상태 분리" 불변식과 무관하다.
- **수정 위험도**: 낮음

---

### [P1] `A/E` 워드마크 클릭이 방금 만든 `#card=` 공유 상태를 지운다

- **위치**: `src/App.jsx:565` — `<a className="wordmark" href="#top">A/E</a>`
- **문제**: 앵커가 `location.hash`를 `#top`으로 덮어써서, 주소창에 실려 있던 카드 설정이 소리 없이 사라진다.
- **재현**:
  1. 문구를 입력하고 `링크 복사`를 누른다 → 주소가 `…/#card=eyJyYXRpbyI6…` 로 바뀐다.
  2. 상단 `A/E` 워드마크를 클릭한다.
  3. 주소가 `…/#top` 이 된다. 카드 정보는 남아 있지 않다.
- **근거**: **실제 브라우저.**
  ```
  {"before":"#card=eyJyYXRpbyI6IjE6MSIs","after":"#top","shareStateLost":true}
  ```
- **영향**: 이 앱은 클립보드 쓰기가 막히면 **"주소창의 주소를 직접 복사해 주세요"** 라고 안내한다(`App.jsx:400`). 실제로 내 세션에서 클립보드 쓰기가 실패해 그 안내가 표시됐다. 그 안내를 따르려고 화면 위쪽으로 돌아가다 워드마크를 누르면, 복사할 주소가 이미 사라진 뒤다. 링크를 받은 사람은 사용자의 카드가 아니라 기본 카드를 보게 된다. 요청 §5 "공유 링크가 기존 `#card=` 상태를 잃지 않는가"에 대한 답은 **잃는다** 이다.
- **제안**: 워드마크를 `.nav-studio-button`과 같은 `<button type="button">`으로 바꾸고 `window.scrollTo({top:0})`를 부른다(이미 `scrollToStudio`가 같은 이유로 앵커 대신 버튼을 쓰고 있다 — 한쪽만 적용됐다). 앵커를 유지해야 한다면 `onClick`에서 `preventDefault()` 후 스크롤만 한다.
- **수정 위험도**: 낮음

---

### [P2] 밝은 화면으로 바꿀 때 `--accent` / `--ok` 토큰을 다시 잡지 않아 12곳 이상이 대비 미달

- **위치**: `src/styles.css:8` (`--accent: #67bce5`), `:10` (`--ok: #2e9c7a`) — `:root`는 아이보리로 바뀌었는데 강조색 토큰은 어두운 배경 기준 그대로다. 꼬리의 "Sky & Ivory" 블록(`:1589-`)이 `.quick-start-kicker`, `.persona-era`, `.ratio-suggestion` 등 **일부만** `#176f98`/`#1b789f`로 손으로 덮었고, 토큰 자체와 나머지 사용처는 남았다.
- **문제**: 하늘색 `#67bce5`는 아이보리(`#f5f0e6`)·패널(`#fffdf7`) 위에서 2.0~2.1:1 밖에 나오지 않는다.
- **재현**: 1440×900에서 페이지를 열고 아래 요소들의 대비를 잰다.
- **근거**: **실제 브라우저** — 전체 DOM을 순회하며 실제 계산 색과 조상 배경으로 WCAG 대비를 계산했다.

  | 요소 | 텍스트 | 크기 | 실측 | 필요 |
  | --- | --- | --- | --- | --- |
  | `.panel > h2 span` | `01` `02` `03` (패널 번호) | 11px | **2.09:1** | 4.5 |
  | `.persona-ratios` ×3 | `추천 1:1 · 4:5` | 9px | **2.05:1** | 4.5 |
  | `.story-kicker` | `DIGITAL IDENTITY THROUGH TIME` | 10px | **2.21:1** | 4.5 |
  | `.notice.success` + `.notice-tag` | `완료 ENTERING ERA / 2004 …` | 14px | **3.36:1** | 4.5 |
  | `.ready-summary.pass`, 체크 `✓` ×4 | `준비 완료` | 11–12px | **3.14:1** | 4.5 |
  | `.era-timeline button[aria-pressed] small` | `숏폼 · 썸네일` (**선택된** 시대) | 9px | **2.51:1** | 4.5 |
  | `.hero-copy h1 em` | `다른 나.` | 56px | **2.68:1** | 3.0 |
  | `.contrast-tag` | 가독성 검사 배지 자신 | 12px | **3.84:1** | 4.5 |

  참고로 **본문 계열은 전부 통과**했다: hero lead 4.69, story lead 4.55, `.hint` 5.23, privacy 4.80, preview-meta 5.23. 문제는 본문이 아니라 강조색 하나다. 아깝게 놓친 것도 있다 — `.brand-kicker` 4.37, `.persona-intro`/`.hint` 4.44, scanner timeline `2004`/`MEMORY` 3.98.
- **영향**: 실패 목록이 하필 **상태를 알리는 것들**이다. 알림 배너(앱의 유일한 피드백 채널), 게시 전 확인의 통과 표시, 현재 선택된 시대의 설명, 패널 진행 번호. 밝은 화면에서 눈에 가장 안 들어와야 할 것이 가장 안 들어온다. 그리고 아이러니하게도 **대비 검사 기능의 배지 자체가 대비 검사를 통과하지 못한다** — 포트폴리오에서 이 기능을 설명할 때 심사자가 바로 눈치챌 지점이다.
- **제안**: 꼬리에서 개별 셀렉터를 손으로 덮는 대신 `:root`에서 `--accent`를 이미 쓰고 있는 `#1b789f`(또는 `#176f98`) 계열로 내리고, `--ok`도 `#1e7a5e` 정도로 내린다. `--accent`가 "면 색"으로 쓰이는 두 곳(`.era-timeline button[aria-pressed=true]` 배경, focus outline)만 확인하면 나머지는 토큰 교체로 한 번에 해결된다. 시대 카드 **내부**의 아이스 블루/더스티 핑크/네온 라임은 별도 스코프(`.scanner-era-*`)라 영향받지 않는다.
- **수정 위험도**: 중간 (토큰이 광범위하게 쓰여 한 번의 전면 확인이 필요하다)

---

### [P2] 포커스 표시가 밝은 배경 위에서 3:1 미만이다

- **위치**: `src/styles.css:157-160` — `:focus-visible { outline: 2px solid var(--accent) }`
- **문제**: 포커스 링 색이 위 항목과 같은 `#67bce5`라, 아이보리 배경 대비 **1.87:1**, 패널 배경 대비 **2.09:1**이다. WCAG 1.4.11 / 2.4.11이 요구하는 3:1에 못 미친다.
- **재현**: Tab으로 아무 버튼에나 초점을 옮기고 링 색을 배경과 비교한다.
- **근거**: **실제 브라우저 측정** — `focusRing_vs_pageBg: 1.87`, `focusRing_vs_panel: 2.09`.
- **영향**: 이 앱은 스크롤이 6,800px가 넘고 컨트롤이 수십 개다. 키보드 사용자가 "지금 어디에 있는지"를 잃는다. 요청 §6-C "포커스 표시가 밝은 배경과 어두운 카드 양쪽에서 보이는가"에 대한 답은 **밝은 쪽에서 잘 안 보인다** 이다. (어두운 `작품으로 보기` 화면에서는 오히려 잘 보인다.)
- **제안**: 위 P2와 같은 토큰 수정으로 자동 해결된다. 별도로 하려면 `:focus-visible`만 `outline-color: #1b789f`로 두고 `outline-offset: 2px`를 유지한다. `.scanner-frame`은 `outline: none` + box-shadow 링이라 별도로 링 대비를 올려야 한다(`:1029-1030`, `:1661-1662`).
- **수정 위험도**: 낮음

---

### [P2] 모바일에서 패널이 `02 → 01 → 03` 순서로 보이고, 보이는 순서와 초점 순서가 어긋난다

- **위치**: `src/styles.css:337-342` — `.panel-preview{order:1} .panel-editor{order:2}`
- **문제**: 1100px 이하에서 CSS `order`로 미리보기를 위로 올렸는데, 패널 제목의 숫자(`01 스타일 만들기`, `02 결과 확인`, `03 저장하기`)와 DOM 순서는 그대로다.
- **재현**: 375×812 또는 390×844에서 페이지를 아래로 내린다.
- **근거**: **실제 브라우저** (375×812).
  ```
  visualOrder: [ "02 결과 확인" @3749, "01 스타일 만들기" @4658, "03 저장하기" @6192 ]
  editorOrder: "2", previewOrder: "1"
  ```
- **영향**: 두 가지다.
  1. **번호가 거꾸로 보인다.** 편집 패널의 `30초 빠른 시작 — 1 모습 / 2 시대 / 3 내 사진`이 "02"라고 적힌 블록 **뒤에** 나온다. 첫 방문자에게 "무엇부터"를 알려주려고 붙인 번호가 오히려 순서를 흐린다.
  2. **WCAG 1.3.2(의미 있는 순서) / 2.4.3(초점 순서) 위반.** 눈으로 보는 순서는 미리보기 → 편집기인데 Tab 초점과 스크린리더 낭독 순서는 편집기 → 미리보기다. 시각 장애가 없는 키보드 사용자에게 초점이 화면 아래로 뛰었다가 위로 되돌아오는 것으로 느껴진다.
- **제안**: "결과 먼저" 판단 자체는 옳다(모바일에서 긴 편집 패널을 왕복하지 않게 하는 좋은 결정이다). 대신 **DOM 순서를 바꿔** `PreviewPanel`을 `EditorPanel` 앞에 두고, 데스크톱에서 `order`로 편집기를 왼쪽으로 보낸다. 그러면 초점 순서와 시각 순서가 어느 폭에서도 일치한다. 패널 번호는 위치가 아니라 역할을 가리키게 문구를 조정하거나(예: `만들기` / `확인` / `저장`), 뷰포트에 따라 번호를 숨긴다.
- **수정 위험도**: 중간 (JSX 순서와 CSS `order`를 함께 바꿔야 하고, `E2E`의 좁은 화면 검사가 있어 회귀는 잡힌다)

---

### [P2] 2004 시대에서 `세로 위치` 슬라이더의 절반이 죽어 있는데 값은 계속 변한다

- **위치**: `src/render/renderCard.js:785` — `const bounds = textBox ?? { …0.9 캔버스… }`, 정의는 `src/state/eras.js:16` (`textBox: {x:.13, y:.7, w:.74, h:.16}`)
- **문제**: 2004은 문구를 다이어리 칸(세로 70~86%) 안에 가둔다. 설계 의도는 옳지만(글이 미니홈피 장식과 겹치면 안 된다), **슬라이더와 숫자 표시는 그 사실을 모른다.** 0~70% 구간에서 값 표시만 바뀌고 그림은 한 픽셀도 안 움직인다.
- **재현**:
  1. 시대 `2004`, 비율 `1:1`.
  2. `문구 배치` 묶음을 펼치고 `세로 위치`를 5% → 30% → 50%로 옮긴다.
  3. 라벨은 `5%` `30%` `50%`로 바뀌지만 캔버스는 완전히 동일하다.
- **근거**: **실제 브라우저 픽셀 해시 비교.**
  ```
  era2004: [ {y:0.05,"5%",pix:4013306180}, {y:0.30,"30%",pix:4013306180}, {y:0.50,"50%",pix:4013306180},
             {y:0.75,"75%",pix:163135384}, {y:0.98,"98%",pix:2831025735} ]
  era2026: 다섯 값 모두 서로 다른 해시  ← 정상
  ```
  `가로 위치`도 75%와 100%가 동일하지만, 죽은 구간이 짧아 체감이 덜하다.
- **영향**: 캔버스에서 직접 끄는 조작도 같은 이유로 위쪽에서 반응하지 않는다. 사용자는 "고장 났나" 또는 "내가 잘못 잡았나"를 먼저 의심한다. `게시 전 확인`도 이 상황을 아무것도 말해 주지 않는다(`✓ 문구가 안전 영역 안에 있어요`라고 표시된다).
- **제안**: 렌더 클램프를 바꾸지 말 것(그 판단이 2004의 정체성이다). 대신 **UI가 진실을 말하게** 한다 — `layout` 결과에 이미 `area`가 있으므로, 요청한 `textY`와 실제 배치가 다르면 `shrink-hint`와 같은 자리에 "2004은 문구를 다이어리 칸에 맞춥니다" 한 줄을 띄우고, 슬라이더 `min`/`max`를 해당 시대의 `textBox` 범위로 좁힌다(`eras.js`가 이미 단일 출처라 파생이 쉽다).
- **수정 위험도**: 낮음 (렌더러는 손대지 않는다)

---

### [P2] 모바일 첫 화면에서 Temporal Scanner가 통째로 접힘 아래에 있다

- **위치**: 화면 — 375×812 및 390×844
- **문제**: 시그니처 인터랙션이 첫 스크롤 없이는 존재조차 보이지 않는다.
- **재현**: 375×812에서 페이지를 열고 스크롤하지 않는다.
- **근거**: **실제 브라우저 측정** (문서 좌표, 뷰포트 812px).

  | 요소 | top |
  | --- | --- |
  | `.brand-kicker` | 147 |
  | `h1` | 190 |
  | `.hero-lead` | 381 |
  | `바로 짤 만들기` 버튼 | 490 |
  | **`.temporal-scanner`** | **672** |
  | **`.scanner-frame` (드래그 대상)** | **783** |
  | `.scanner-timeline` (2004/2012/2026) | 1267 |
  | **`ENTER ERA` CTA** | **1457** |

  첫 화면에 들어오는 것은 킥커·제목·리드·버튼 하나뿐이다. 드래그 프레임은 812px 경계에 29px만 걸친다.
- **영향**: 요청 §4-2, §6-A에 직접 걸린다. 모바일에서 이 페이지의 첫인상은 **"AI 랜딩페이지"와 구분되지 않는다** — 큰 세리프 제목, 회색 리드 한 문단, 파란 버튼 하나. 제품을 다른 것으로 만드는 요소가 전부 아래에 있다. 채용 담당자가 링크를 폰으로 여는 경우가 드물지 않고, 10초 안에 스크롤을 두 번 하리라 기대할 수는 없다. `ENTER ERA`는 거의 두 화면 아래다.
- **제안**: `.masthead { min-height: min(790px, calc(100vh - 32px)) }`(`:1601`)와 `.hero-copy { padding-block: 70px 54px }`(`:1636`)가 원인이다. 640px 이하에서 hero-copy의 세로 패딩을 절반으로 줄이고 `h1`을 `clamp(40px, 12vw, 52px)` 정도로 낮춰 프레임 상단을 600px 아래로 끌어올리면, 스캐너 프레임의 위쪽 절반이 첫 화면에 들어온다. 그것만으로도 "여기 뭔가 조작할 게 있다"가 전달된다. 데스크톱(1440×900)에서는 이 문제가 없다 — 스캐너가 hero 오른쪽 열에 함께 보인다.
- **수정 위험도**: 낮음

---

### [P2] `prefers-reduced-motion`에서도 부드러운 스크롤이 강제된다

- **위치**: `src/App.jsx:452`(`enterHeroEra`), `:461`(`scrollToStudio`) — `scrollIntoView({ behavior: 'smooth' })`
- **문제**: `styles.css:591-598`의 reduced-motion 블록은 `scroll-behavior: auto !important`를 넣지만, JS가 **호출 인자로** `behavior:'smooth'`를 넘기면 CSS는 무시된다.
- **재현**: OS에서 "동작 줄이기"를 켜고 `바로 짤 만들기` 또는 `ENTER ERA`를 누른다. 6,800px 문서를 부드럽게 훑고 내려간다.
- **근거**: **코드 추론** + reduced-motion 블록 확인. (내 환경에서 OS 설정을 실제로 토글하지는 못했다.)
- **영향**: 전정기관 장애가 있는 사용자에게 가장 긴 스크롤 두 개가 그대로 남는다. reduced-motion 대응을 이미 넣어 둔 프로젝트라 더 아쉬운 누락이다. 다만 **핵심 정보가 사라지지는 않는다** — 세 시대 카드도, 스캐너 판독값도 정적으로 읽힌다.
- **제안**: `const motionOk = !window.matchMedia('(prefers-reduced-motion: reduce)').matches;` 를 두고 `behavior: motionOk ? 'smooth' : 'auto'`로 넘긴다. 두 줄이다.
- **수정 위험도**: 낮음

---

### [P2] 시대별 전체 화면 강조색이 꼬리 override로 무력화됐고, 문서는 아직 살아 있다고 말한다

- **위치**: `src/styles.css:876-878` vs `:1593-1599`
  ```css
  /* 876-878 — 원래 의도 */
  .app.era-2004 { --era-glow: #7dd9ff; … }
  .app.era-2012 { --era-glow: #ff9ab6; … }
  .app.era-2026 { --era-glow: #b8ff5c; … }

  /* 1593-1599 — 셋을 같은 값으로 눌러버린다 */
  .app, .app.era-2004, .app.era-2012, .app.era-2026 { --era-glow: #55add8; … }
  ```
- **문제**: `App.jsx:557`의 `className={`app era-${state.era}`}`가 하는 일이 없다.
- **재현**: 시대를 바꾸고 `getComputedStyle(document.querySelector('.app')).getPropertyValue('--era-glow')`를 읽는다.
- **근거**: **실제 브라우저** — `.app era-2004` 상태에서 `--era-glow: #55add8`. 세 시대 모두 동일.
- **영향**: `docs/PORTFOLIO-CASE-STUDY.md`는 여전히 **"현재 선택한 시대가 전체 화면의 강조색에 반영된다"** 를 판단 근거로 제시한다. 심사자가 확인하면 사실이 아니다 — 케이스 스터디의 다른 주장(전부 검증했다)까지 신뢰가 흔들린다. 코드 쪽으로도 세 줄의 CSS 규칙과 하나의 React className이 죽은 채 남아 있다.
- **제안**: 결정해서 한쪽만 남긴다. (a) 시대 강조를 되살린다면 세 색을 아이보리 위에서 4.5:1 나오는 값으로 다시 잡고 꼬리 override를 지운다. (b) 밝은 화면 통일을 유지한다면 `:876-878`과 `App.jsx`의 `era-${state.era}` 클래스를 지우고 케이스 스터디 문장을 수정한다. **(b)를 권한다** — 현재 방향은 "페이지 UI는 아이보리·하늘색·네이비 중심"이고, 시대 개성은 이미 스캐너 카드 내부와 Canvas 결과물이 충분히 지고 있다.
- **수정 위험도**: 낮음

---

### [P2] 시대 메타데이터가 네 곳에 중복되어 있다

- **위치**:
  1. `src/state/eras.js:8-36` — `id`, `label`, `caption`(`개인 홈 · 메신저`), `renderKind`, 박스
  2. `src/components/TemporalScanner.jsx:4-29` — `STOPS`: `year`, **`ratio`**, `label`(`MEMORY`/`FEED`/`SIGNAL`), `title`, `meta`
  3. `src/App.jsx:437-441` — `enterHeroEra`의 `choices`: 시대별 **추천 persona와 ratio를 하드코딩**
  4. `src/App.jsx:600-611` — 소개 섹션의 `01 — MEMORY` / `02 — FEED` / `03 — SIGNAL` 문구
- **문제**: `eras.js`가 "시대 축의 단일 정의"라고 선언하고 있지만, 화면에 실제로 보이는 시대 정보의 대부분은 그 파일에 없다.
- **근거**: **코드 추론.** 지금은 값이 우연히 일치한다 — `enterHeroEra`의 `2004→1:1`, `2012→4:5`, `2026→9:16`이 `STOPS`의 `ratio`와도, `presets.getRecommendedRatio()`가 계산하는 값과도 같다. 세 계산 경로가 독립적으로 같은 답을 내고 있을 뿐이다.
- **영향**: 요청 §6-G가 정확히 이 지점을 묻는다. `COMBINATION_STYLES`에서 `social:2026`의 `recommendedRatio`를 `4:5`로 바꾸면, 미리보기의 추천 배지는 `4:5`로 바뀌지만 Hero CTA는 계속 `9:16`을 적용한다 — 같은 화면이 두 가지 답을 준다. 새 시대(예: 2018)를 추가하려면 최소 4개 파일과 스캐너의 `continuousYear`/`frameRatio` 보간 함수(`TemporalScanner.jsx:39-47`, 구간이 2개로 하드코딩됨)까지 손대야 한다.
- **제안**: `ERA_DEFINITIONS`에 `scannerLabel`(MEMORY/FEED/SIGNAL), `heroPersona`, `heroTitle`, `heroMeta`를 얹고, `STOPS`와 `choices`와 소개 섹션이 그것을 읽게 한다. `ratio`는 이미 `getRecommendedRatio(persona, era)`가 계산하므로 `choices`에서 지우고 그 함수를 부르면 중복 하나가 바로 사라진다. 불변식과 무관한 순수 리팩터링이며 `test/eras.test.js`가 회귀를 잡는다.
- **수정 위험도**: 중간

---

### [P2] CSS가 팔레트 두 벌을 겹쳐 쓰고 있고, 뒤쪽 280줄이 앞쪽을 덮는다

- **위치**: `src/styles.css` 1,869줄. 특히 `:1316-1588` "Cinematic editorial type system"(어두운 전시형)과 `:1589-1869` "Sky & Ivory design system"(현재 방향)
- **문제**: 밝은 화면으로의 전환이 **앞쪽 규칙 수정이 아니라 뒤에 덧칠하는 방식**으로 이루어졌다. 같은 셀렉터가 두 번 정의되고, 나중 것이 이긴다.
- **근거**: **코드 추론 + 실제 브라우저.** 구체적 증거 셋:
  - `.scanner-frame`이 `:1013`, `:1656`, `:1013 이후 미디어쿼리`에서 세 번 정의된다.
  - 위 P2(대비)와 P2(era-glow)가 모두 이 구조의 직접적 산물이다 — 꼬리에서 손으로 덮다가 `.persona-ratios`와 `--accent` 토큰을 빠뜨렸다.
  - DOM에 존재하지 않는 죽은 셀렉터: `.hero-cta`(`:974-981`), `.timeline-track`(`:1167`), `.local-badge`(`:44-53`, `:584`), `.era-card`, `.top-nav a:last-child`(`:917`), `.masthead h1`(`:41`, 지금 h1은 `.hero-copy h1`이다).
- **영향**: 유지보수 위험이다. 다음 사람이 색 하나를 바꾸려면 파일에서 그 셀렉터가 몇 번 나오는지부터 세야 한다. 실제로 이번 리뷰에서 발견한 대비 결함 대부분이 "꼬리에서 덮는 걸 빠뜨렸다"로 설명된다. 포트폴리오 심사자가 CSS를 열어 볼 확률은 낮지만, 코드 리뷰 단계까지 가면 눈에 띈다.
- **제안**: 지금 당장 전면 정리할 필요는 없다(제출 임박이면 위험 대비 이득이 낮다). 최소 조치는 세 가지다 — (1) `:root` 토큰을 밝은 팔레트 기준으로 다시 잡아 꼬리의 하드코딩 색을 토큰 참조로 되돌린다, (2) 위에 나열한 죽은 셀렉터 6개를 지운다, (3) `:1316-1588` 블록에서 꼬리가 전부 덮은 규칙을 걷어낸다. 제출 이후 작업으로 미뤄도 되지만 **기록은 남겨야 한다.**
- **수정 위험도**: 높음 (전면 정리 시) / 낮음 (죽은 셀렉터 제거만)

---

### [P2] 공개 페이지에 og:image·favicon이 없고 `theme-color`가 옛 어두운 값이다

- **위치**: `index.html:11` (`<meta name="theme-color" content="#080b0c">`), 그리고 없는 것들
- **문제**: 배포된 페이지를 실제로 조회한 결과다.
  ```
  title:       "ALTER / EGO — Time-Travel Identity Studio"        ✓
  description: "같은 사진을 2004·2012·2026의 …"                    ✓
  og:title/description/type/locale                                 ✓
  theme-color: "#080b0c"   ← 거의 검정. 페이지는 #f5f0e6 아이보리
  og:image:    (없음)
  og:url:      (없음)
  twitter:card:(없음)
  favicon:     (없음)
  ```
- **근거**: **실제 브라우저** — https://myeongjundev.github.io/t03-card-studio/ 에서 직접 읽음. (같은 조회로 공개 페이지가 밝은 재디자인 반영본임을 확인했다: `bodyBg = rgb(245,240,230)`.)
- **영향**:
  - **og:image 없음이 가장 크다.** 포트폴리오 링크는 Slack·카톡·노션·이메일로 전달된다. 지금은 제목과 설명만 있는 회색 카드로 펼쳐진다. 이 프로젝트는 시각 작업물이고, 85KB짜리 대표 배너(`짤스튜디오배너.avif`)가 이미 저장소에 있다. 링크 미리보기가 첫인상을 결정하는 상황에서 가장 값싼 개선이다.
  - `theme-color: #080b0c`는 어두운 전시형 시절 잔재다. 모바일 Chrome/Safari가 아이보리 페이지 위에 새까만 브라우저 바를 그린다.
  - favicon이 없어 탭에 기본 지구본이 뜬다.
- **제안**: `theme-color`를 `#f5f0e6`으로 바꾸고, `og:image`(절대 URL 필요 — `og:url`과 함께), `twitter:card: summary_large_image`, `<link rel="icon">`을 추가한다. og:image는 크롤러가 AVIF를 못 읽는 경우가 있어 **1200×630 JPEG/PNG 파생본 한 장**을 `public/`에 두는 편이 안전하다.
- **수정 위험도**: 낮음

---

### [P2] E2E 테스트 픽스처 191KB가 공개 사이트에 배포되고 있다

- **위치**: `public/sample/` → `dist/sample/`
- **문제**: `public/`은 Vite가 그대로 복사한다. 그 안의 파일은 테스트 전용이다.
- **근거**: **빌드 산출물 실측.**

  | 파일 | 크기 | 앱이 참조? |
  | --- | --- | --- |
  | `dist/assets/짤스튜디오배너-DaHLCwQ4.avif` | **85,578 B** | ✓ Archive 배너 |
  | `dist/assets/짤스튜디오-olt-w5Q5.avif` | **52,004 B** | ✓ Scanner 인물 |
  | `dist/sample/transparent-circle-600x600.png` | 63,435 B | ✗ |
  | `dist/sample/portrait-800x1400.png` | 58,541 B | ✗ |
  | `dist/sample/landscape-1600x600.png` | 43,685 B | ✗ |
  | `dist/sample/landscape-1200x450.jpg` | 25,783 B | ✗ |

  `grep -rn "sample/" src/ index.html` → 없음. 참조하는 곳은 `test/e2e/pixels.e2e.js`뿐이다.
- **영향**: 배포본에 **191KB의 죽은 자산**이 실린다. 두 히어로 이미지(137KB)를 합친 것보다 크다. README의 "성능" 절이 "3.8MB → 138KB"를 성과로 내세우는데, 정작 배포본에는 그 성과보다 큰 테스트 픽스처가 들어 있다. 페이지 로드는 이 파일들을 요청하지 않으므로 **체감 성능에는 영향이 없다** — 배포 위생과 서술의 일관성 문제다.
- **참고 (긍정)**: 원본 PNG 3.8MB는 번들에 들어가지 않았다 — 번들 문자열을 확인한 결과 `.avif` 두 개만 참조된다. AVIF 크기도 프롬프트가 말한 52KB·85.57KB와 정확히 일치한다. §6-F의 두 질문 중 하나는 통과다.
- **제안**: `public/sample/`을 `test/fixtures/sample/`로 옮기고 `test/e2e/server.js`가 그쪽을 서빙하게 한다. 또는 `vite.config.js`의 빌드 후크에서 `dist/sample`을 지운다. 앞쪽이 깨끗하다.
- **수정 위험도**: 낮음 (E2E가 즉시 회귀를 잡는다)

---

### [P2] README에 현재 UI 스크린샷이 없고, 대표 이미지가 2MB PNG다

- **위치**: `README.md:9` — `![…](./배너이미지/짤스튜디오배너.png)`
- **문제**: 두 가지가 겹친다.
  1. 그 파일은 **2,049,839 B (2.0MB)** 다. AVIF 파생본(85KB)이 바로 옆에 있는데도 README는 원본 PNG를 건다. 저장소 첫 화면이 2MB를 받는다. (`짤스튜디오.png` 1.76MB까지 합쳐 저장소에 3.8MB의 원본이 커밋되어 있다.)
  2. 그 이미지는 **앱 화면이 아니라 콘셉트 아트워크**다. 방금 전체 페이지를 아이보리·하늘색으로 재디자인했는데, README 어디에도 그 화면이 없다.
- **근거**: **파일 실측** + README 통독.
- **영향**: 채용 담당자의 절반 이상은 `Live Demo`를 누르기 전에 README를 훑는다. 지금 그들이 보는 것은 인물 콜라주 한 장이다. 이 프로젝트의 실제 차별점 — 드래그 가능한 Temporal Scanner, 3분할 스튜디오, 게시 전 확인 — 은 한 장도 보이지 않는다. 요청 §6-H "README 대표 이미지가 현재 UI를 오해하게 만들지는 않는가"에 대한 답은 **오해까지는 아니지만 아무것도 알려주지 않는다** 이다.
- **제안**: 대표 이미지를 AVIF(또는 새로 만든 1200px JPEG)로 교체하고, 그 아래에 **(a) 드래그 중인 Scanner(두 시대가 경계 양쪽에 보이는 순간), (b) Studio 3분할 전체** 스크린샷 2장을 추가한다. 이 프로젝트에서 가장 설명하기 어려운 것이 "Scanner가 장식이 아니다"인데, 중간 드래그 상태 스크린샷 한 장이 문단 세 개보다 낫다.
- **수정 위험도**: 낮음

---

### [P3] `게시 전 확인`의 `fit` 항목 라벨이 실제 검사 내용과 다르다

- **위치**: `src/state/readyCheck.js:38-44`
- **문제**: 이 항목은 **자동 축소 여부**(`layout.shrunk`)를 본다. 그런데 통과 라벨이 `문구가 안전 영역 안에 있어요`다. 안전 영역은 바로 아래 `vertical-safe-area` 항목이 따로 검사한다.
- **재현**: 9:16 비율로 두면 목록에 "안전 영역"이 두 번 나오고 서로 다른 것을 뜻한다. 1:1에서는 안전 영역 검사를 하지도 않으면서 "안전 영역 안에 있어요"라고 말한다.
- **근거**: **코드 추론** + **실제 브라우저** (1:1/2004에서 `✓ 문구가 안전 영역 안에 있어요` 표시 확인).
- **영향**: 상태 알림이 실제 상태와 어긋난다(요청 §6-C). 사용자가 이 도구의 판정을 신뢰하기 어려워진다. 게다가 위의 P2(2004 죽은 슬라이더) 상황에서 이 항목이 `✓`를 띄워 "문제 없음"이라고 오도한다.
- **제안**: 라벨을 `문구가 지정한 크기 그대로 들어갔어요` / `긴 문구가 자동으로 축소됐어요`로 바꾼다. 문구만 고치면 된다.
- **수정 위험도**: 낮음

---

### [P3] Scanner 판독값의 `aria-live="polite"`가 드래그 중 스크린리더를 뒤덮는다

- **위치**: `src/components/TemporalScanner.jsx:110`
- **문제**: `.scanner-readout`이 `aria-live="polite"`인데 안에 소수점 둘째 자리까지의 연도(`2024.60`)가 들어 있다. 키를 누르고 있거나 드래그하는 동안 값이 수십 번 바뀐다.
- **근거**: **코드 추론** + 실측(키 누름 12회에 `2026.00 → 2017.60`, 매 프레임 갱신).
- **영향**: 스크린리더 사용자가 연속 낭독에 묻힌다. 게다가 `.scanner-frame`이 이미 `role="slider"` + `aria-valuetext`를 갖고 있어 **같은 정보를 두 경로로 두 번** 알린다.
- **제안**: `.scanner-readout`에서 `aria-live`를 빼고(`role="slider"`의 `aria-valuetext`가 이미 담당한다), 스냅이 끝난 시점에만 별도의 `aria-live` 영역에 `2012년으로 고정` 같은 결과 문장을 한 번 넣는다.
- **수정 위험도**: 낮음

---

### [P3] `role="slider"`의 값 범위가 실제 매핑과 비선형이다

- **위치**: `src/components/TemporalScanner.jsx:127-130`, `:39-42`
- **문제**: `aria-valuemin="2004" aria-valuemax="2026"`인데 `continuousYear()`는 progress 0.5를 2012로 보낸다. 보조기술이 위치를 백분율로 환산하면 (2012−2004)/22 = **36%** 인데, 화면의 경계선은 **50%** 에 있다.
- **근거**: **코드 추론** + 실측(progress 0.25 → `--time-position: 50%`, 판독값 `SCANNING 2004 → 2012`).
- **영향**: 실용적 피해는 작다(대부분의 스크린리더는 `aria-valuetext`를 우선 읽는다). 다만 정지점이 등간격이 아니라는 사실이 어디에도 드러나지 않는다.
- **제안**: `aria-valuemin="0" aria-valuemax="2" aria-valuenow={phase + localProgress}`로 "정지점 인덱스"를 값으로 삼고, 사람이 읽는 연도는 `aria-valuetext`에 남긴다.
- **수정 위험도**: 낮음

---

### [P3] 값이 바뀌지 않는 조작도 되돌리기 단계를 만든다

- **위치**: `src/App.jsx:250`(`update`), `:415`(`usePreset`), `:425`(`useEra`)
- **문제**: 세 함수 모두 항상 새 객체를 만든다. 이미 선택된 Persona를 다시 누르거나 슬라이더를 같은 값으로 되돌려도 `lastStateRef.current !== state`가 되어 history에 한 단계가 쌓인다. `usePreset`/`useEra`는 `isolateBurst`까지 붙어 있어 **묶이지도 않고 매번 독립 단계**가 된다.
- **재현**: `소셜`을 다섯 번 연속 클릭한 뒤 `실행 취소`를 누른다. 다섯 번 눌러야 이전 상태로 돌아간다.
- **근거**: **코드 추론.** (Undo/Redo의 본질적 동작은 실제 브라우저에서 정상 확인했다 — 아래 §5 참조.)
- **영향**: 되돌리기 60단계 한도가 의미 없는 단계로 빨리 찬다. 사용자는 "취소를 눌렀는데 아무것도 안 바뀐다"를 겪는다.
- **제안**: `clampState` 결과를 이전 상태와 얕게 비교해 모든 키가 같으면 이전 객체를 그대로 반환한다. `history.js`는 손대지 않아도 되고 `test/history.test.js`가 그대로 유효하다.
- **수정 위험도**: 낮음

---

### [P3] 비활성 컨트롤과 작은 터치 타깃

- **위치**: `src/styles.css:1791` (`button:disabled { color:#9cafb8; background:#eef2f2 }`), `Slider` 컴포넌트
- **근거**: **실제 브라우저 측정** (390×844).
  - `button:disabled` 대비 **2.02:1**. WCAG 1.4.3은 비활성 컨트롤을 면제하므로 규정 위반은 아니지만, `이미지 다운로드`/`템플릿으로 저장`이 왜 눌리지 않는지 알아보기 어렵다.
  - `input[type=range]` 5개가 높이 **22px**, `.dock-restore`("크게") 32×23px — WCAG 2.5.8 최소 24×24px에 못 미친다.
- **영향**: 모바일에서 문구 위치·크기 조절이 주 편집 수단인데 트랙이 22px다. 손가락으로 잡기가 까다롭다.
- **제안**: 비활성 텍스트를 `#6b7e88` 정도로 올리고, 640px 이하에서 range의 `height`(또는 `padding-block`)를 24px 이상으로 키운다.
- **수정 위험도**: 낮음

---

### [P3] Canvas 폰트 스택이 웹폰트를 쓰지 않아 결정성이 "같은 기기 안에서만" 성립한다

- **위치**: `src/render/renderCard.js:15-17` — `FONT_STACK`
- **문제**: `'Malgun Gothic', 'Apple SD Gothic Neo', 'Noto Sans KR', …` 순서라 Windows는 Malgun Gothic, macOS는 Apple SD Gothic Neo, 그 외에는 Google에서 받은 Noto Sans KR로 그린다. 줄바꿈 위치와 자간이 OS마다 달라진다.
- **근거**: **실제 브라우저** — 렌더 직후 `ctx.font`가 `bold 32.34px "Malgun Gothic", …` 로 확인됨(Windows).
- **영향**: "같은 설정을 두 번 렌더하면 같은 PNG" 불변식은 **한 기기 안에서는 완전히 성립한다**(E2E `같은 설정으로 다시 그리면 필름 입자까지 똑같다`가 이를 증명한다). 다만 공유 링크를 다른 OS에서 열면 카드가 미묘하게 다르게 보인다. 앱이 `document.fonts.ready`를 기다리는 이유가 정확히 줄바꿈 안정성인데, 정작 캔버스가 쓰는 폰트는 그 대기와 무관한 로컬 폰트다.
- **제안**: 기능적 결함은 아니다. 크로스 플랫폼 일치가 목표라면 `FONT_STACK` 맨 앞을 `'Noto Sans KR'`로 올린다(이미 Google에서 로드 중이다). **다만 이는 의도적 판단일 수 있으므로**, 유지한다면 ARCHITECTURE에 "결정성은 동일 환경 기준"이라고 한 줄 명시하는 편이 정직하다.
- **수정 위험도**: 중간 (렌더 골든 픽스처가 함께 갱신되어야 한다)

---

### [P3] 사용되지 않는 ref

- **위치**: `src/components/TemporalScanner.jsx:53` — `const scannerRef = useRef(null)`. `:121`에서 붙지만 읽는 곳이 없다.
- **근거**: **코드 추론** (`grep scannerRef` → 정의와 대입 두 줄뿐).
- **영향**: 없음. 다음 사람이 "무엇에 쓰였나"를 찾는 시간만 든다.
- **제안**: 삭제.
- **수정 위험도**: 낮음

---

# 확인했으나 결함을 찾지 못한 영역

아래는 **"문제 없음"이 아니라 "이 방법으로 이 범위까지 확인했고 그 안에서는 발견하지 못했다"** 는 뜻이다.

### 미리보기와 다운로드가 같은 픽셀 — 위반 없음
`src/io/exportImage.js` 전체를 읽었다. `downloadCanvas`는 인자로 받은 캔버스를 `toBlob()` 할 뿐이고, `copyCanvasImage`도 같은 캔버스를 쓴다. `document.createElement('canvas')`나 재렌더 경로는 파일 전체에 없다. `PreviewPanel`은 `width={size.width} height={size.height}`(1080×H)로 내부 해상도를 잡고 `.canvas-frame`의 CSS `width`로만 축소한다. E2E `미리보기 캔버스와 내보낸 PNG 가 같은 픽셀이다`가 실제 픽셀로 이를 확인했다(11.9초, 통과).

### JSON 전체 검증 후 저장 — 위반 없음
`App.jsx:340-378`의 `importJson`이 `readTemplatesFile` → `result.ok` 확인 → `window.confirm` → `commitTemplates` 순서다. 검증 실패 시 `setNotice`만 하고 반환한다. `src/templates/schema.js`는 저장소를 import조차 하지 않는 순수 함수다. `test/schema.test.js` 및 `test/storage.test.js`의 관련 케이스 전부 통과.

### 오류가 사용자 데이터를 지우지 않는다 — 위반 없음
**실제 브라우저에서 재현했다.** 문구 `오늘도 무사히 하루를 잘 마무리했습니다 😀`를 입력한 뒤 (1) `text/plain` 파일 투입 → 형식 오류 알림, 문구 그대로. (2) MIME은 `image/png`지만 내용이 쓰레기인 5바이트 파일 투입 → `이미지를 읽지 못했습니다` 알림, 문구 그대로. 두 경우 모두 편집 상태가 보존됐다. `storage.js`의 격리 로직도 `pendingCorruptValue` 가드로 "격리 실패 시 후속 저장 차단"까지 구현되어 있고 해당 단위 테스트가 통과한다.

### Canvas 렌더가 동기·결정적 — 위반 없음
`App.jsx:78-113`의 렌더 effect에 `requestAnimationFrame`이 없다(주석에 그 판단 근거까지 적혀 있다). `grep Math.random src/render/` → 0건. 난수는 `seededRandom(0x5ca1ab1e)`와 `seededRandom(0x9e3779b9)` 두 고정 씨앗뿐이다. E2E `같은 설정으로 다시 그리면 필름 입자까지 똑같다` 통과. (단, OS 간 폰트 차이는 위 P3 참조.)

### Hero 탐색과 편집 상태 분리 — 위반 없음
**실제 브라우저에서 재현했다.** 문구를 입력한 상태에서 Scanner를 2004로 드래그·스냅해도 편집 패널의 시대는 `2026`, persona `기본`, 문구 그대로였다. `ENTER ERA`를 눌렀을 때만 시대 `2004` + persona `친한 친구` + 비율 `1:1`이 적용되고, **문구 `오늘도 무사히 😀`는 그대로 유지**됐다. 알림도 "사진과 문구는 그대로 유지했습니다"로 정확했다.

### 이미지 외부 전송 없음 — 위반 없음
`src/`, `index.html`, `vite.config.js` 전체에서 `fetch(` / `XMLHttpRequest` / `sendBeacon` / `WebSocket` 을 검색해 **0건**. 외부 URL은 Google Fonts 3개(preconnect 2 + stylesheet 1)뿐이다. `shareLink.js`의 `SHARED_FIELDS`에 `image`/`imageName`이 없고, `test/shareLink.test.js:88`이 `secret-photo.png`가 링크에 실리지 않음을 검사한다. 공유는 `#` 해시라 서버로 전송되지 않는다 — 그 이유가 주석에 정확히 적혀 있다.

### Temporal Scanner가 장식이 아닌지 — 실제로 동작한다
**실제 브라우저에서 중간 드래그 상태를 계측했다.** progress 0.25(2004↔2012 중간)에서:
```
scanner-era-2004  is-base    clip-path: inset(0px)              opacity 1  z 1
scanner-era-2012  is-reveal  clip-path: inset(0px 50% 0px 0px)  opacity 1  z 2
scanner-era-2026  is-hidden                                     opacity 0  z 0
TIME CUT: left 132px (data-edge="middle")   readout: "SCANNING 2004 → 2012"
```
두 시대가 경계선 양쪽에 **실제로 동시에** 렌더링된다. 케이스 스터디의 서술과 구현이 일치한다. 요청 §8-3 시나리오 통과. **이것은 장식이 아니라 진짜 시그니처 인터랙션이다.**

### Undo/Redo 묶음 분리 — 정상
**실제 브라우저에서 검증했다.**
```
타이핑 burst 2회 → persona '소셜' 클릭
undo 1회 → persona 만 '친한 친구' 로 (문구 XYZ 유지)   ← Persona가 독립 단계
undo 2회 → 문구 AAABC 로                               ← 타이핑 burst가 한 단계
redo 1회 → 문구 XYZ 복원, canRedo=true
새 편집   → canRedo=false                              ← redo 가지 안전하게 제거
```
`isolateBurst`의 "앞뒤 양쪽을 끊는다" 설계가 실제로 동작한다.

### 27개 조합에서 이미지 영역이 캔버스를 벗어나는가 — 벗어나지 않는다
`getComposition` × `isBoxInsideCanvas`로 Persona 3 × Era 3 × Ratio 3 = **27개 전부**를 계산했다. `imageBox`·`textBox` 모두 위반 0건. 2004의 `{x:.13, y:.25, w:.74, h:.36}`은 어떤 비율에서도 캔버스 안이다.

### 투명 배경에서 시대 장식이 불투명 픽셀을 남기는가 — 남기지 않는다
`drawComposition`(`renderCard.js:663`)과 `drawPersonaEmphasis`(`:731`) 양쪽에 `if (state.transparentBg) return;` 가드가 있고, `drawText`도 `transparentBg`일 때 `textBox`를 `null`로 넘겨 시대가 강제하는 문구 칸까지 제거한다. `test/eraTransparency.test.js`가 세 시대의 그리기 기록이 **완전히 동일**하고 `clearRect`/`fillText`/`strokeText` 외에는 아무것도 없음을 검사한다. E2E `투명 배경을 켜면 알파가 실제로 보존된다`도 통과.

### 2004 이미지 보정 캐시가 잘못 재사용되는가 — 재사용되지 않는다
`phoneFrameCache`는 `WeakMap<HTMLImageElement, Map<string, OffscreenCanvas>>`이고 캐시 키가 `${fit}:${bufferWidth}x${bufferHeight}`다. 출력에 영향을 주는 변수는 이미지(맵 키), `fit`, 버퍼 크기(= `imageBox` 종횡비) 셋뿐이며 전부 키에 반영되어 있다. 색보정 파라미터는 전부 상수다. 재사용 오류 경로를 찾지 못했다.

### 가로 넘침 — 없음
**실제 브라우저에서 전체 DOM을 순회**하며 뷰포트를 벗어나는 요소를 찾았다. 375×812 → 0건(`scrollWidth 375`), 390×844 → 0건(`scrollWidth 390`), 1440×900 → 0건(`scrollWidth 1430`). 제목의 부자연스러운 줄바꿈이나 잘린 컨트롤도 발견하지 못했다. 375px에서 `h1`은 56.25px / 325px 폭으로 정상 배치된다. **768×1024와 1100px 전후는 계측하지 못했다** — §"확인하지 못한 범위" 참조.

### 성능 — 체감 저하를 재현하지 못했다
`PerformanceObserver({entryTypes:['longtask']})`로 50ms 초과 태스크를 감시하면서, **9:16(1080×1920, 가장 무거운 조건)** 에서 세 시대 각각 15회 연속 문구 입력을 실행했다.
```
era2026_9x16_typing            longTasks: 0
era2012_9x16_typing_filmgrain  longTasks: 0   ← 필름 입자 + getImageData 대비 검사
era2004_9x16_typing            longTasks: 0
```
**긴 태스크 0건.** 작은 버퍼(300px)에서만 픽셀 연산을 하고, 그레인은 128px 타일을 패턴으로 반복하며, 2004 보정을 캐시하는 세 가지 판단이 실제로 효과를 내고 있다. Scanner 드래그는 CSS 변수 갱신뿐이라 구조적으로 비용이 없다. (프레임 렌더 자체는 브라우저 pane이 표시되지 않아 rAF 기반 측정을 하지 못했다 — longtask 관측으로 대신했다.)

### 모바일 플로팅 미리보기 — 정상 동작
**실제 브라우저 (390×844).** `편집하며 보기`를 누르면 `.canvas-stage`가 `position: fixed`, 148×148, 우하단 12/12, `z-index: 20`으로 떠오르고 내부 캔버스가 134px로 축소된다. 다시 누르면 `static`으로 복귀한다. README의 "편집 중 플로팅 미리보기 지원"은 사실이다.

### 30초 빠른 시작 순서 — 일치한다
`EditorPanel`의 안내(`1 모습 / 2 시대 / 3 내 사진`)와 실제 패널 배치(`persona-picker` → `era-picker` → `DropZone`)가 정확히 같은 순서다(`EditorPanel.jsx:75-144`). 다만 위 P2(모바일 패널 순서 역전)로 인해 모바일에서는 이 안내에 도달하기 전에 미리보기를 먼저 만난다.

### API Key / 토큰 / 개인정보 — 발견하지 못했다
추적 중인 전 파일과 **58개 커밋 전체의 blob**을 대상으로 `api[_-]?key|secret|password|token|AKIA…|ghp_…|sk-…|BEGIN … PRIVATE KEY|Bearer `를 검색했다. 매칭은 전부 정상 식별자였다 — `App.jsx:224`의 input type 목록에 있는 `'password'`, `wrapText.js`의 지역 변수 `token`, `deploy.yml`의 `id-token: write`(GitHub OIDC 표준), 테스트 픽스처 파일명 `secret-photo.png`. `.env`/`.pem`/`.key`/credential 류가 커밋된 이력도 **없다**.

### `git diff --check` — 깨끗함
공백 오류 0건, 종료 코드 0.

---

# 종합

## 1. 최종 판정 — **CONDITIONAL YES**

포트폴리오에 넣을 수준이다. 다만 **제출 전에 P1 두 개는 고쳐야 한다.**

근거를 나눠 적는다.

**넣을 만한 이유.** 이 프로젝트는 "기능이 많은 편집기"가 아니라 **판단이 보이는 제품**이다. 그리고 그 판단이 말이 아니라 코드와 테스트로 증명되어 있다 — 미리보기와 다운로드를 같은 캔버스로 통일한 것, rAF를 되돌린 것(그리고 그 이유가 주석에 남아 있는 것), 손상된 localStorage를 지우지 않고 격리하는 것, 프리셋이 비율을 말없이 바꾸지 않고 추천만 하는 것. 세 번째는 특히 드물다. 대부분의 지원자는 "프리셋을 누르면 다 알아서 바뀝니다"를 자랑으로 적는다. 여기서는 "말없이 바꾸면 무엇이 바뀐 건지 알 수 없다"를 이유로 **일부러 하지 않았고**, 그 이유를 `presets.js:16-18`에 적어 뒀다. 면접에서 30분을 채울 수 있는 종류의 결정이다.

그리고 Temporal Scanner는 실제로 동작한다. 이 리뷰에서 가장 확인하고 싶었던 것이 "시그니처 인터랙션이 스크린샷용 장식인가"였는데, 중간 드래그 상태를 계측한 결과 두 시대가 정말로 한 프레임 안에 클리핑되어 공존한다. 케이스 스터디의 서술이 마케팅이 아니다.

**조건이 붙는 이유.** 밝은 화면으로의 전환이 **끝나지 않았다.** `--accent`와 `--ok` 토큰이 어두운 배경 시절 값 그대로이고, 꼬리 280줄에서 손으로 덮다가 여러 곳을 빠뜨렸다. 그 결과 알림 배너·게시 전 확인 통과 표시·선택된 시대 설명·패널 번호가 전부 대비 미달이다. 이것은 "취향"이 아니라 측정값이다. 그리고 심사자가 키보드를 잡으면 Scanner의 방향키가 아무것도 하지 않는다 — README가 명시적으로 광고하는 기능이다.

**NO가 아닌 이유.** 위 문제들이 전부 **표면 수정**이다. 아키텍처는 건강하다. 불변식 여섯 개가 하나도 깨져 있지 않고, 단위 70개와 E2E 12개가 전부 통과하며, 내가 직접 재현을 시도한 데이터 손실 시나리오가 모두 방어됐다. 고칠 것은 색 토큰 두 개, 이벤트 핸들러 한 줄, 앵커 하나다.

## 2. 10초 첫인상

**데스크톱(1440×900) — 통과한다.** 왼쪽에 `같은 사진. / 다른 시대. / 다른 나.`가 94px로 서고, 오른쪽에 조작 가능한 프레임이 나란히 놓인다. 시선은 제목 → Scanner → CTA 순으로 간다. **이 순서가 맞다** — 무엇에 관한 것인지 먼저 알고, 그다음 만져 볼 것을 본다. `◀ DRAG TIME ▶` 큐와 아래의 `2004 | 2012 | 2026` 타임라인이 "이건 조작하는 물건"임을 즉시 알린다. 10초 안에 "한 장의 사진을 세 시대로 다시 그리는 도구"가 전달된다.

**모바일(375×812) — 통과하지 못한다.** 첫 화면에 들어오는 것은 킥커 한 줄, 제목 세 줄, 회색 리드 한 문단, 파란 버튼 하나가 전부다. Scanner 프레임은 y=783에서 시작해 화면 끝(812)에 29px만 걸친다. 이 조합은 지난 2년간 쏟아진 AI 랜딩페이지와 구분되지 않는다. 제품을 특별하게 만드는 모든 것이 접힘 아래에 있다.

**차별점이 즉시 이해되는가**: 데스크톱에서는 그렇다. 다만 한 가지가 약하다 — 첫 화면에서 `바로 짤 만들기`(왼쪽)와 `2026로 짤 만들기`(오른쪽)가 **둘 다 "짤 만들기"** 라고 말한다. 하나는 시대를 안 정한 채 스튜디오로 가고, 하나는 시대를 적용하고 간다. 무엇이 다른지 화면에서 알 수 없다. `또는 오른쪽에서 시대를 먼저 탐색하세요`가 그 차이를 설명하려 하지만 11px 회색 메타 텍스트다. 왼쪽 버튼을 `시대 없이 바로 시작`처럼 결과를 말하는 문구로 바꾸면 두 경로가 즉시 갈린다.

## 3. 출시·제출을 막는 문제

**막는 것은 두 개다.**

1. **[P1] Scanner 방향키 무효.** 키보드 사용자에게 시그니처 인터랙션이 없는 것과 같고, README가 광고하는 기능이며, 공개 페이지에서 재현된다.
2. **[P1] 워드마크가 `#card=` 공유 상태를 지운다.** 앱이 스스로 안내한 대체 경로(주소창 복사)를 앱이 스스로 무너뜨린다.

**막지는 않지만 제출 품질을 눈에 띄게 깎는 것**: 강조색 대비 미달(알림·상태 표시·선택된 시대), 포커스 링 1.87:1, 모바일 첫 화면에 Scanner 부재, og:image 없음.

## 4. 제출 전 고치면 효과가 큰 세 가지

**① `--accent`와 `--ok` 토큰을 밝은 배경 기준으로 다시 잡는다** (수정 위험도 중간, 실작업 30분)
`:root`의 두 값을 이미 꼬리에서 쓰고 있는 `#1b789f` / 더 진한 초록 계열로 내린다. 이 한 번의 수정이 12곳 이상의 대비 실패와 포커스 링 문제를 **동시에** 해결한다. 개성은 잃지 않는다 — 시대 카드 내부의 아이스 블루·더스티 핑크·네온 라임은 `.scanner-era-*` 스코프라 손대지 않는다. 오히려 페이지 UI가 한 단계 차분해지면 시대 카드의 색이 더 튄다. **가장 적은 수정으로 가장 많은 결함이 사라지는 항목이다.**

**② P1 두 개를 고친다** (수정 위험도 낮음, 실작업 15분)
`onKeyUp={snap}` 제거, 워드마크를 버튼으로 교체. 각각 한 줄이다. Scanner 쪽은 `test/`에 회귀 테스트를 함께 넣기를 권한다(§6 참조).

**③ 모바일 hero를 한 화면 안으로 끌어올리고 README에 현재 UI 스크린샷을 넣는다** (수정 위험도 낮음, 실작업 1시간)
640px 이하에서 `.hero-copy`의 세로 패딩을 줄이고 `h1`을 낮춰 Scanner 프레임 상단을 접힘 위로 올린다. 동시에 README 대표 이미지를 AVIF로 바꾸고 **중간 드래그 상태의 Scanner 스크린샷**을 추가한다. 이 프로젝트에서 가장 설명하기 어려운 것이 "Scanner가 장식이 아니다"인데, 두 시대가 경계 양쪽에 보이는 한 장이 문단 세 개보다 낫다. og:image도 같은 이미지로 해결된다.

## 5. 현재 그대로 유지해야 할 설계 판단

아래는 **건드리지 말 것을 권한다.**

- **다운로드가 미리보기 캔버스를 그대로 `toBlob()` 하는 것.** 두 렌더러를 "맞추는" 대신 하나만 두는 판단이다. 이 프로젝트에서 가장 값진 한 줄이다.
- **동기 렌더.** rAF로 되돌리자는 제안이 나오면 거절해야 한다. 이유(백그라운드 탭에서 빈 캔버스 저장)가 `App.jsx:71-77`에 이미 적혀 있고, 성능 측정 결과 긴 태스크가 0건이라 되돌릴 이유도 없다.
- **프리셋이 비율을 적용하지 않고 추천만 하는 것.** `applyIdentity`가 `ratio`와 `recommendedRatio`를 **둘 다** 구조분해로 떼어 내는 방어(`presets.js:163`)까지 포함해서. "나중에 조합을 추가하는 사람이 습관적으로 ratio를 적더라도"라는 주석이 정확히 옳다.
- **손상된 localStorage를 삭제하지 않고 격리하는 것**, 그리고 **격리에 실패하면 후속 저장을 차단하는 것**(`pendingCorruptValue`). 후자는 대부분의 구현이 놓치는 부분이다.
- **`isolateBurst`의 앞뒤 양방향 차단.** "Persona를 고른 뒤 0.6초 안에 문구를 옮기면 되돌리기 한 번에 둘이 함께 사라진다"를 실제로 겪고 고친 흔적이다. 실측으로 정상 동작을 확인했다.
- **`willReadFrequently: true`를 켜는 것.** 이유(GPU→CPU 전환 시 안티에일리어싱이 미세하게 달라져 결정성이 깨진다)가 대부분의 사람이 모르는 수준이다.
- **투명 배경에서 시대가 강제하는 `textBox`까지 제거하는 것.** 장식만 끄는 것으로는 부족하다는 것을 알아챈 판단이다.
- **모바일에서 결과 패널을 위로 올리는 판단 자체.** 위 P2는 이 판단을 되돌리라는 것이 아니라 **DOM 순서로 구현하라**는 뜻이다.
- **`docs/PORTFOLIO-CASE-STUDY.md`의 서술 방식.** 기능 나열이 아니라 문제 → 판단 → 검증 가능한 결과 순이다. "면접에서 설명할 핵심" 절은 그대로 두는 것이 좋다(단, era-glow 문장 한 줄은 수정 필요 — 위 P2).

## 6. 부족한 테스트 우선순위

현재 82개(단위 70 + E2E 12)는 **렌더 정확성과 데이터 안전성에 잘 쏠려 있다.** 비어 있는 쪽은 **상호작용과 접근성**이다. 이번에 찾은 P1/P2 중 자동 검사에 걸린 것이 하나도 없다는 사실이 그 증거다.

1. **Scanner 키보드 조작 (P1을 잡았을 테스트).** `.scanner-frame`에 초점을 주고 `ArrowLeft`를 **keydown+keyup 쌍으로** 눌렀을 때 `aria-valuenow`가 실제로 변하는지. E2E 한 개면 된다. **최우선.**
2. **`#card=` 상태 보존.** 공유 링크를 만든 뒤 페이지 내 내비게이션(워드마크, `바로 만들기`, `ENTER ERA`)을 모두 거쳐도 해시가 살아남는지. P1 두 번째를 잡는다.
3. **대비 회귀 검사.** 이번에 쓴 방식(DOM 순회 + 실제 계산 색/조상 배경으로 WCAG 비율 계산)을 E2E에 넣고 임계값 미달 요소가 0개임을 단언한다. `checkTextContrast`로 **캔버스 안**은 이미 검사하는데 **페이지 UI**는 검사하지 않는다는 비대칭이 지금 상태를 만들었다.
4. **시대 메타데이터 일관성 (단위).** 모든 `ERA_KEYS`에 대해 `TEMPORAL_STOPS`의 `ratio`와 `enterHeroEra`의 `choices`와 `getRecommendedRatio()`가 같은 답을 내는지. 위 P2의 drift를 구조적으로 막는다.
5. **초점 순서 = 시각 순서 (E2E, 375px).** 패널 세 개의 `getBoundingClientRect().top` 순서와 Tab 초점 진행 순서가 일치하는지.
6. **시대별 문구 클램프의 유효 범위 (단위).** 2004에서 `textY`를 0~1로 훑으며 실제 렌더 `area.y`가 몇 개의 서로 다른 값을 갖는지. 죽은 구간이 생기면 즉시 드러난다.
7. **동일 값 재적용이 되돌리기 단계를 만들지 않는지 (단위).** P3의 회귀 방지.

## 7. 실제로 실행한 명령과 결과

```powershell
git status --short              # 출력 없음 — 작업트리 깨끗함
git log -5 --oneline            # HEAD = f15f754 (기준 6b1b4a4 보다 1커밋 앞, 문서만)
git branch --show-current       # main
npm.cmd test                    # tests 70 / pass 70 / fail 0 / duration 321.4ms
npm.cmd run build               # ✓ built in 153ms
npm.cmd run test:e2e            # tests 12 / pass 12 / fail 0 / duration 54.5s
git diff --check                # 출력 없음, exit 0
```

**빌드 산출물 크기**

```
dist/index.html                             1,380 B  (gzip 0.75 kB)
dist/assets/index-drY52Kyw.js             258,801 B  (gzip 82.59 kB)
dist/assets/index-C4LdqFhZ.css             49,326 B  (gzip 11.11 kB)
dist/assets/짤스튜디오배너-DaHLCwQ4.avif      85,578 B   ← 프롬프트 85.57KB 와 일치
dist/assets/짤스튜디오-olt-w5Q5.avif          52,004 B   ← 프롬프트 52KB 와 일치
dist/sample/*.png, *.jpg                  191,444 B   ← 앱이 쓰지 않음 (위 P2)
```

**E2E 12개 개별 결과** — 전부 통과: 미리보기/PNG 픽셀 일치(11.9s) · 9:16 안전영역 가이드 미포함 · 세 시대 픽셀 차이 · 투명 알파 보존 · 페이지 오류 없음 · 재렌더 필름입자 일치 · 2004 화질 변환 · 드래그앤드롭 · 좁은 화면 가로 스크롤 없음 · 접이식 묶음 · 사진 위 모습 변화 · 아홉 조합 가독성 기준.

**추가로 실행한 검증**

- 로컬 dev 서버(`localhost:5173`)와 **공개 페이지 양쪽**에서 브라우저 계측.
- Node로 27개(Persona×Era×Ratio) 조합의 `imageBox`/`textBox` 캔버스 이탈 검사 → 0건.
- 58개 커밋 전체 blob 대상 시크릿 스캔 → 0건.
- 전체 DOM WCAG 대비 순회(1440×900, 375×812, 390×844).
- 뷰포트 이탈 요소 순회(375 / 390 / 1440).
- `PerformanceObserver` longtask 감시하 3시대 × 15회 연속 입력(9:16).
- Scanner 키보드/포인터 이벤트 시뮬레이션 및 clip-path·CSS 변수 계측.
- Undo/Redo 시나리오, 잘못된 파일 2종, 공유 링크 왕복 실증.

## 8. 확인하지 못한 범위와 이유

**정직하게 적는다.**

1. **모든 스크린샷.** 이 세션의 브라우저 pane이 표시되지 않아 컴포지팅이 멈춰 있었고, `screenshot`이 5초 타임아웃으로 계속 실패했다. **따라서 "예뻐 보이는가"에 대한 최종 판단은 하지 못했다.** 시각 관련 결론은 전부 계측값(좌표, 크기, 계산된 색, 대비 비율, clip-path, 폰트 크기)에서 도출한 것이고, 색의 조화나 이미지 자체의 완성도는 판단 범위 밖이다. 위 §2의 "데스크톱 첫인상 통과"는 요소 배치와 타이포 크기에 근거한 것이지 실제 그림을 본 것이 아니다.
2. **768×1024와 1100px 전후 뷰포트.** 375·390·1440만 계측했다. 1100px는 레이아웃 분기점(`max-width:1100`과 `min-width:1101`) 바로 위아래라 **가장 깨지기 쉬운 구간인데 확인하지 못했다.** 코드상 `.hero`가 1101px에서 `minmax(360px,.82fr) minmax(560px,1.18fr)`로 바뀌고 `.app` 내부 폭이 1053px라 최소값 합(920px+gap)은 들어가지만, 실측하지 않았다.
3. **`prefers-reduced-motion`의 실제 동작.** OS 설정을 토글할 수 없어 코드 추론에만 근거했다. CSS 블록이 존재하고 JS가 `behavior:'smooth'`를 하드코딩한다는 사실은 확인했지만, 실제로 어떻게 느껴지는지는 보지 못했다.
4. **실제 스크린리더(NVDA/VoiceOver).** ARIA 속성값과 라이브 영역 구조는 읽었지만 낭독을 들어보지 못했다. `aria-live` 폭주(P3)는 구조에서 추론한 것이다.
5. **실제 사진 업로드 후의 시각 품질.** 손상 파일 두 종으로 오류 경로는 검증했지만, 정상 사진을 올렸을 때 2004 보정·2012 필름 그레인이 "그 시절처럼 보이는지"는 픽셀을 보지 못해 판단하지 못했다. E2E가 "세 시대가 실제 픽셀로 다르다"와 "2004는 사진을 그 시절 화질로 바꾼다"를 통과하므로 **다르다는 것은 확실하지만, 설득력 있는지는 다른 문제다.**
6. **PNG 다운로드와 이미지 복사의 실제 파일.** E2E가 `toBlob` 결과를 픽셀 비교하므로 불변식은 검증됐지만, 브라우저에서 직접 내려받아 파일을 열어 보지는 않았다. 클립보드 쓰기는 이 환경에서 권한이 막혀 실패했다(앱은 올바르게 대체 안내를 표시했다).
7. **템플릿 CRUD와 JSON 왕복의 브라우저 실증.** 단위 테스트(`storage.test.js`, `schema.test.js`)가 격리·중복ID·legacy 이관·범위 검증을 모두 덮고 있어 그 결과에 의존했다. 파일 선택 대화상자가 필요한 가져오기 경로는 브라우저에서 실행하지 못했다.
8. **배너 이미지의 인물.** 실제 인물 사진인지 생성 이미지인지 판별할 수 없었다. 실제 인물이라면 공개 배포에 동의가 필요하다 — 확인을 권한다.

**리뷰 중 만든 임시 파일**: 로컬 dev 서버를 띄우기 위해 `.claude/launch.json`을 만들었고, 리뷰 종료 시 **삭제했다.** 최종 `git status`는 깨끗하다. 이 보고서 파일(`CLAUDE_REVIEW_2026-08-26.md`) 외에 저장소를 변경하지 않았다.
