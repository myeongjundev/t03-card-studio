# SKT ALEPH T03 · 짤·카드 스튜디오
## Claude / Codex 작업 전 필독 문서

> 이 문서는 T03 과제를 구현하기 전에 Claude와 Codex가 반드시 읽어야 하는 작업 원칙이다.  
> 목적은 단순히 기능을 많이 만드는 것이 아니라, **과제 요구사항을 정확히 이해하고, 강사가 짧은 시간 안에 통과 여부를 판단할 수 있으며, 실제 사용자가 안정적으로 사용할 수 있는 결과물**을 만드는 것이다.

---

# 1. 이번 과제를 대하는 마음가짐

이번 과제는 “이미지에 글자를 넣는 페이지 하나”를 만드는 일이 아니다.

우리가 보여줘야 하는 것은 다음과 같다.

- 요구사항을 정확히 해석하는 능력
- 사용자가 이해하기 쉬운 화면을 설계하는 능력
- 입력값 변화가 즉시 화면에 반영되는 상태 관리
- 미리보기와 실제 다운로드 결과를 일치시키는 구현 능력
- 실패하기 쉬운 입력을 미리 예상하고 검증하는 태도
- 사용자 데이터를 안전하게 보존하는 예외 처리
- 생성·수정·삭제가 실제로 작동하는 데이터 관리
- JSON 백업·복원 과정의 검증
- AI가 만든 결과를 그대로 받아들이지 않고 직접 판단하고 검증하는 능력

따라서 목표는 **“작동하는 척하는 데모”가 아니라 “검증 가능한 완성품”**이다.

기능 하나를 구현할 때마다 다음 질문을 스스로 해야 한다.

> 이 기능은 실제로 작동하는가?  
> 강사가 공개 주소에서 바로 확인할 수 있는가?  
> 새로고침하거나 잘못된 입력을 넣어도 데이터가 안전한가?  
> 미리보기에서 보이는 결과와 다운로드 결과가 정말 같은가?  
> 처음 접속한 사람도 설명 없이 무엇을 해야 하는지 알 수 있는가?

---

# 2. 가장 중요한 원칙 — 과제 카드가 최우선이다

디자인, 애니메이션, 멋있는 효과보다 **과제 카드 1~5의 통과 기준이 우선**이다.

작업 중 새로운 아이디어가 생기더라도 다음 순서를 지킨다.

1. 필수 요구사항 충족
2. 실제 동작 검증
3. 예외 상황 검증
4. 강사 판정 동선 정리
5. 그 이후 디자인과 추가 기능 개선

멋있지만 요구사항을 충족하지 못하는 기능은 가치가 없다.

반대로 화려하지 않더라도 요구사항을 정확히 만족하고 안정적으로 작동한다면 좋은 결과물이다.

---

# 3. 강사는 오래 기다려주지 않는다

이 과제에는 중요한 조건이 있다.

- 공개 주소에서 바로 실행 가능해야 한다.
- 검증 행동은 3단계 이내여야 한다.
- 강사가 짧은 시간 안에 통과 여부를 판단할 수 있어야 한다.

따라서 핵심 기능을 여러 페이지 깊숙한 곳에 숨기지 않는다.

첫 화면에서 최소한 다음 요소가 명확하게 보여야 한다.

- 이미지 불러오기
- 문구 입력
- 문구 위치 조절
- 글자 크기 조절
- 글자 색상 변경
- 1:1 / 4:5 / 9:16 비율 선택
- 현재 미리보기
- 템플릿 생성 / 불러오기 / 수정 / 삭제
- JSON 가져오기 / 내보내기
- 완성 이미지 내려받기
- 공개 정보 및 개인정보 주의 안내

**“기능이 존재한다”와 “강사가 기능의 존재를 바로 알 수 있다”는 서로 다른 문제다.**

둘 다 만족해야 한다.

---

# 4. T03의 핵심 구조를 잊지 않는다

이번 과제의 본질은 다음 다섯 가지다.

## 카드 1 — 편집과 미리보기

이미지를 불러오고 한글 문구를 입력한 뒤,

- 위치
- 크기
- 색상

을 변경하면 **즉시 미리보기에 반영**되어야 한다.

입력을 변경했는데 별도 적용 버튼을 여러 번 눌러야 하거나, 미리보기가 늦게 갱신되는 구조는 피한다.

---

## 카드 2 — 화면과 파일의 일치

지원 비율:

- 1:1
- 4:5
- 9:16

미리보기와 다운로드 파일에서 다음 요소가 같아야 한다.

- 이미지 잘림
- 이미지 배치
- 문구 위치
- 문구 크기
- 문구 색상
- 줄바꿈

가능하면 **미리보기와 다운로드가 동일한 렌더링 로직을 사용하도록 설계**한다.

화면용 구현과 저장용 구현을 따로 만들어 서로 다른 결과가 나오는 구조는 최대한 피한다.

---

## 카드 3 — 극단 입력 검사

정상적인 예제만 테스트하고 끝내지 않는다.

최소 12개 이상의 실패하기 쉬운 입력을 검증한다.

예:

1. 매우 긴 한글
2. 매우 긴 영어
3. 한글 + 영어 혼합
4. 여러 줄 문장
5. 이모지
6. 특수문자
7. 빈 문구
8. 매우 큰 글자
9. 매우 작은 글자
10. 세로 이미지
11. 가로 이미지
12. 투명 PNG

중요한 것은 **12개를 넣었다는 사실 자체가 아니라 결과를 확인했다는 것**이다.

문구가 잘리거나 겹치는 결함이 발견되면 숨기지 않는다.

오히려

> 결함 발견 → 원인 분석 → 수정 → 수정 후 재검증

과정을 남긴다.

이것이 과제에서 요구하는 실질적인 테스트 역량이다.

---

## 카드 4 — 실제 템플릿 관리

미리 만들어 둔 템플릿 3개를 보여주는 것만으로는 통과가 아니다.

사용자가 실제로 다음 작업을 할 수 있어야 한다.

- 생성
- 불러오기
- 수정
- 삭제

그리고 새로고침 후에도 유지되어야 한다.

템플릿에는 최소한 다음 정보를 고려한다.

- 템플릿 ID
- 이름
- 화면 비율
- 문구
- 문구 위치
- 문구 크기
- 문구 색상
- 이미지 맞춤 방식
- 필요한 경우 배경/레이아웃 설정

저장된 데이터와 UI 상태가 서로 어긋나지 않도록 주의한다.

---

## 카드 5 — 옮겨 쓰기 / JSON 백업·복원

템플릿을 JSON으로 내보낼 수 있어야 한다.

그리고 정상 JSON을 다시 가져오면 설정이 복원되어야 한다.

그러나 가장 중요한 것은 **잘못된 JSON을 넣었을 때 기존 데이터를 보호하는 것**이다.

절대 다음과 같이 구현하지 않는다.

> 파일 선택 → 기존 템플릿 삭제 → JSON 파싱 시도

반드시 다음 순서를 따른다.

> 파일 읽기 → JSON 파싱 → 구조 검증 → 값 검증 → 전체 검증 성공 → 저장

검증에 실패하면:

- 오류 이유를 사용자에게 표시한다.
- 현재 편집 내용을 유지한다.
- 기존 템플릿을 유지한다.
- localStorage 또는 저장 데이터를 덮어쓰지 않는다.

---

# 5. 절대로 하면 안 되는 구현

다음은 금지에 가깝게 생각한다.

## 5.1 가짜 기능

버튼은 있지만 실제 동작하지 않는 상태를 만들지 않는다.

예:

- “삭제” 버튼이 목록에서만 없어지고 실제 저장 데이터에는 남아 있음
- “수정” 버튼이 새 템플릿을 추가하는 방식으로 동작
- JSON 가져오기 버튼이 파일 선택만 하고 실제 복원을 하지 않음
- 다운로드 버튼이 미리보기와 다른 이미지를 생성함

---

## 5.2 하드코딩으로 요구사항을 속이지 않는다

카드 4의 핵심은 **사용자가 직접 템플릿을 관리하는 것**이다.

기본 예제 템플릿을 제공하는 것은 가능하지만 그것을 CRUD 구현의 대체물로 사용하지 않는다.

---

## 5.3 오류가 발생했다고 기존 데이터를 삭제하지 않는다

잘못된 이미지, 잘못된 JSON, 지원하지 않는 파일 등 어떤 오류에서도 사용자의 기존 작업을 최대한 보존한다.

실패한 입력은 실패한 입력만 거부한다.

---

## 5.4 디자인을 먼저 만들고 기능을 억지로 끼워 넣지 않는다

예쁜 화면보다 먼저 데이터 흐름과 렌더링 구조를 안정화한다.

특히 다음 기능이 먼저다.

- 이미지 로드
- Canvas 렌더링
- 텍스트 렌더링
- 비율
- 다운로드
- 템플릿 저장
- JSON 검증

---

## 5.5 AI의 결과를 무조건 정답으로 취급하지 않는다

Claude와 Codex의 제안은 참고 자료다.

AI가 다음과 같이 말하더라도 직접 검증한다.

- “구현 완료”
- “모든 테스트 통과”
- “미리보기와 다운로드가 동일”
- “JSON 검증 완료”
- “반응형 대응 완료”

코드가 존재하는 것과 실제 브라우저에서 정상 작동하는 것은 다르다.

---

# 6. AI에게 맡길 일과 사람이 판단할 일

## AI에게 적극적으로 맡겨도 되는 것

- 컴포넌트 분리
- 반복 코드 정리
- Canvas 렌더 함수 구현
- 상태 관리 코드
- localStorage CRUD
- JSON schema 검증 로직
- 테스트 케이스 초안
- 에러 메시지 초안
- 코드 리팩터링
- 접근성 속성 확인
- 빌드 오류 분석

## 반드시 사람이 최종 판단할 것

- 화면이 처음 보는 사람에게 이해되는가
- 강사가 15~30초 안에 판정할 수 있는가
- 어떤 기능을 첫 화면에 배치할 것인가
- 디자인 방향
- 문구와 설명의 자연스러운 한국어
- 테스트에서 실제로 결함이 발생했는가
- 미리보기와 다운로드 결과가 눈으로 보아 같은가
- 개인정보 또는 비밀값이 노출되지 않았는가
- 제출할 증거가 과장되지 않았는가

---

# 7. Claude와 Codex의 역할

두 AI가 동시에 코드를 무작정 수정하지 않는다.

가능하면 역할을 나눈다.

## Claude

주로 다음 역할에 활용한다.

- 전체 구조 검토
- 요구사항 누락 검사
- UX 흐름 점검
- 구현 아이디어 비교
- 테스트 케이스 설계
- 코드 리뷰
- 문제 원인 분석
- 문서와 검증 안내서 검토

## Codex

주로 다음 역할에 활용한다.

- 실제 코드 작성
- 파일 생성 및 수정
- 컴포넌트 구현
- Canvas 기능 구현
- localStorage CRUD
- Import / Export
- 오류 수정
- 빌드 및 테스트
- 리팩터링

단, 역할은 고정 규칙이 아니다.

중요한 것은 **두 AI 모두 과제 전체 요구사항을 이해한 상태에서 작업하도록 하는 것**이다.

---

# 8. 한 번에 전부 만들지 않는다

“이 요구사항 전부 구현해줘”라고 한 번에 요청하면 누락이 발생하기 쉽다.

아래 단계처럼 진행한다.

## Phase 1 — 기반

- 프로젝트 구조
- 첫 화면 레이아웃
- 상태 모델
- Canvas 렌더링 기반

## Phase 2 — 카드 1

- 이미지 업로드
- 문구 입력
- 위치
- 크기
- 색
- 실시간 미리보기

## Phase 3 — 카드 2

- 1:1
- 4:5
- 9:16
- 동일 렌더러 기반 다운로드

## Phase 4 — 카드 4

- 템플릿 생성
- 불러오기
- 수정
- 삭제
- 새로고침 유지

## Phase 5 — 카드 5

- JSON 내보내기
- JSON 가져오기
- schema 검증
- 잘못된 파일 보호
- 이미지 다운로드

## Phase 6 — 카드 3

- 극단 입력 12개
- 실제 테스트
- 대표 결함 확인
- 수정 전/후 기록

## Phase 7 — 최종 품질

- 디자인
- 반응형
- 접근성
- 에러 메시지
- 사용 안내
- 공개 배포
- 제출 증거

---

# 9. 구현할 때 항상 데이터 흐름을 생각한다

UI만 보고 개발하지 않는다.

예:

```text
사용자 입력
   ↓
Editor State
   ↓
Validation
   ↓
Render Function
   ↓
Canvas
   ├─ 화면 Preview
   └─ Image Export
```

템플릿도 마찬가지다.

```text
현재 Editor State
   ↓
Template 생성
   ↓
Validation
   ↓
LocalStorage
   ↓
Template 목록
   ↓
불러오기
   ↓
Editor State 복원
```

가능한 한 **하나의 상태를 여러 곳에서 중복 관리하지 않는다.**

---

# 10. Canvas 작업 시 특히 주의할 점

이번 과제에서 Canvas를 사용한다면 다음을 반드시 확인한다.

- 원본 이미지가 완전히 로드된 후 그린다.
- 화면 비율 변경 시 내부 Canvas 크기도 올바르게 변경한다.
- CSS 크기와 실제 Canvas 해상도를 혼동하지 않는다.
- 다운로드는 실제 Canvas 데이터를 사용한다.
- 텍스트 좌표 기준을 통일한다.
- 글꼴 로딩이 완료된 뒤 export 한다.
- 긴 문장은 영역에 맞게 줄바꿈한다.
- Emoji 및 한글 fallback font를 확인한다.
- 투명 PNG의 alpha 값을 유지한다.
- 이미지 `contain / cover` 동작을 명확하게 정의한다.

---

# 11. 사용자 데이터 보호

이 과제의 이미지는 브라우저에서 다룰 수 있도록 가능한 한 로컬 처리를 우선한다.

불필요하게 이미지를 외부 서버로 전송하지 않는다.

또한 제출 화면이나 저장 데이터에 다음 정보가 포함되지 않게 한다.

- 이름
- 전화번호
- 이메일
- 주소
- 위치 정보
- API Key
- Token
- Password
- OAuth Secret
- 개인 사진의 EXIF 위치 정보

테스트용 이미지도 직접 만든 이미지 또는 사용 권한이 명확한 이미지를 사용한다.

---

# 12. 지원하지 않는 입력에 대한 태도

오류를 숨기지 않는다.

좋은 프로그램은 모든 입력을 억지로 처리하는 프로그램이 아니라,

**처리할 수 없는 입력을 안전하게 거부하고 그 이유를 알려주는 프로그램**이다.

예:

> 지원하지 않는 파일 형식입니다. PNG 또는 JPEG 파일을 선택해주세요. 기존 작업은 유지됩니다.

> JSON 구조가 올바르지 않아 가져오기를 중단했습니다. 기존 템플릿은 변경되지 않았습니다.

이런 메시지가 필요하다.

---

# 13. 테스트는 마지막 장식이 아니다

테스트는 구현의 일부다.

기능을 만든 직후 바로 확인한다.

예:

### 이미지 업로드 구현 후

- PNG
- JPEG
- 큰 이미지
- 작은 이미지
- 지원하지 않는 파일

### 텍스트 구현 후

- 한글
- 영어
- 혼합
- 줄바꿈
- 이모지
- 긴 문장

### 템플릿 구현 후

- 생성
- 불러오기
- 수정
- 삭제
- 새로고침
- 중복 이름

### JSON 구현 후

- 정상 파일
- 깨진 JSON
- 필수 필드 누락
- 잘못된 타입
- 예상하지 못한 값

---

# 14. 테스트 통과를 말하기 전에 직접 증명한다

AI가 테스트 코드만 작성했다고 “통과”라고 기록하지 않는다.

가능하면 실제 브라우저에서 직접 확인한다.

특히 다음은 눈으로 비교한다.

- 미리보기
- 다운로드 이미지

다음은 실제 새로고침으로 확인한다.

- 템플릿 유지

다음은 실제 잘못된 파일을 입력하여 확인한다.

- 기존 데이터 보호

---

# 15. 한국어 UX를 중요하게 본다

AI가 생성한 번역투 문장을 그대로 사용하지 않는다.

다음처럼 자연스러운 한국어를 사용한다.

나쁜 예:

> 당신의 템플릿은 성공적으로 저장되어졌습니다.

좋은 예:

> 템플릿을 저장했습니다.

버튼도 가능한 한 행동이 바로 이해되게 작성한다.

- 템플릿 저장
- 변경 내용 저장
- 템플릿 삭제
- JSON 가져오기
- JSON 내보내기
- 이미지 다운로드

---

# 16. 디자인 원칙

디자인은 기능을 돕기 위해 존재한다.

이번 T03은 제작 도구이므로 다음 세 영역이 명확하게 분리되어야 한다.

```text
편집 영역 | 미리보기 | 템플릿/파일 관리
```

사용자가 현재 무엇을 편집하는지 항상 알 수 있게 한다.

비율 선택도 현재 활성 상태를 명확히 표시한다.

버튼이 많아지는 만큼 중요도에 따라 시각적 위계를 둔다.

---

# 17. 욕심낼 기능과 버릴 기능을 구분한다

시간이 남으면 고려할 수 있는 기능:

- Drag & Drop 텍스트 이동
- 텍스트 정렬
- 여러 문구 레이어
- 이미지 필터
- Undo / Redo
- 키보드 단축키
- 기본 프리셋
- 공유용 카드 스타일
- 레트로/보안 콘셉트

그러나 필수 요구사항이 완성되기 전에는 구현하지 않는다.

---

# 18. 프로젝트가 깨지는 큰 수정은 신중하게 한다

작동하는 기능을 리팩터링할 때는 먼저 현재 동작을 확인한다.

특히 다음 파일 또는 구조를 동시에 대규모로 변경하지 않는다.

- 상태 모델
- Canvas 렌더러
- Template schema
- localStorage schema
- Import / Export schema

변경 후 바로 빌드하고 테스트한다.

---

# 19. Git 작업 원칙

가능하면 기능 단위로 작업 기록을 남긴다.

예:

```text
feat: implement image and text editor
feat: add canvas ratio presets
feat: add image export
feat: add template CRUD
feat: add JSON import export
fix: preserve templates on invalid JSON
fix: wrap long mixed-language text
test: verify 12 edge cases
docs: add verification guide
```

큰 기능 여러 개를 하나의 거대한 커밋에 몰아넣지 않는다.

---

# 20. AI가 수정하기 전에 확인할 것

Claude 또는 Codex는 파일을 수정하기 전에 먼저 다음을 확인한다.

1. 현재 프로젝트 구조
2. 이미 구현된 기능
3. 사용 중인 라이브러리
4. 상태 관리 방식
5. Canvas 렌더링 방식
6. 저장 데이터 구조
7. 기존 기능과 충돌 가능성

이미 정상 동작하는 기능을 이유 없이 새 구조로 갈아엎지 않는다.

---

# 21. “완료”의 정의

코드가 작성되었다고 완료가 아니다.

아래 조건이 충족되어야 완료다.

- 브라우저에서 실행된다.
- 공개 주소에서 열린다.
- 이미지 편집 기능이 첫 화면에 보인다.
- 입력 변경이 즉시 Preview에 반영된다.
- 세 화면비가 작동한다.
- 다운로드 결과가 Preview와 일치한다.
- 극단 입력 12개를 검사했다.
- 대표 결함의 수정 전/후가 있다.
- 템플릿 CRUD가 실제로 작동한다.
- 새로고침 후 템플릿이 유지된다.
- JSON Export가 작동한다.
- 정상 JSON Import가 작동한다.
- 잘못된 JSON에서 기존 데이터가 유지된다.
- 완성 이미지 3개가 정상적으로 열린다.
- 개인정보/비밀값이 노출되지 않는다.
- 검증 안내서를 작성할 수 있는 상태다.

---

# 22. 마지막에 반드시 확인할 체크리스트

- [ ] PNG 이미지가 정상적으로 열린다.
- [ ] JPEG 이미지가 정상적으로 열린다.
- [ ] 지원하지 않는 파일에서 기존 작업이 유지된다.
- [ ] 문구 수정이 즉시 반영된다.
- [ ] 위치 변경이 즉시 반영된다.
- [ ] 크기 변경이 즉시 반영된다.
- [ ] 색상 변경이 즉시 반영된다.
- [ ] 1:1 비율이 정상이다.
- [ ] 4:5 비율이 정상이다.
- [ ] 9:16 비율이 정상이다.
- [ ] 미리보기와 다운로드 결과가 일치한다.
- [ ] 긴 한글이 잘리지 않는다.
- [ ] 한글/영어 혼합 문장이 정상이다.
- [ ] 줄바꿈이 정상이다.
- [ ] Emoji가 정상이다.
- [ ] 세로 이미지가 정상이다.
- [ ] 투명 PNG가 정상이다.
- [ ] 극단 입력 12개 검사 기록이 있다.
- [ ] 대표 결함 수정 전/후 기록이 있다.
- [ ] 템플릿을 생성할 수 있다.
- [ ] 템플릿을 불러올 수 있다.
- [ ] 템플릿을 수정할 수 있다.
- [ ] 템플릿을 삭제할 수 있다.
- [ ] 새로고침 후 템플릿이 유지된다.
- [ ] JSON을 내보낼 수 있다.
- [ ] 정상 JSON을 가져올 수 있다.
- [ ] 깨진 JSON을 안전하게 거부한다.
- [ ] 필수 값이 빠진 JSON을 안전하게 거부한다.
- [ ] 잘못된 JSON에서도 기존 템플릿이 유지된다.
- [ ] 완성 이미지 3개가 정상적으로 열린다.
- [ ] 공개 주소에 개인정보가 없다.
- [ ] 코드에 API Key / Token / Secret이 없다.
- [ ] 처음 방문한 사람이 기능을 이해할 수 있다.
- [ ] 강사가 15~30초 안에 핵심 기능을 확인할 수 있다.

---

# 23. 이번 과제에서 우리가 보여주고 싶은 것

T03을 단순한 과제 제출물로 끝내지 않는다.

결과물을 본 사람이 다음과 같이 느낄 수 있도록 만든다.

> 요구사항을 제대로 읽었다.  
> 단순히 화면만 만든 것이 아니라 데이터 흐름을 생각했다.  
> 실패 상황을 예상했다.  
> 사용자 데이터를 함부로 삭제하지 않는다.  
> 테스트를 실제로 했다.  
> AI를 사용했지만 AI에게 끌려다니지는 않았다.  
> 본인이 판단하고 검증하면서 개발했다.

---

# 24. AI에게 가장 중요한 지시

Claude와 Codex는 아래 원칙을 항상 기억한다.

> **요구사항을 줄이거나 임의로 바꾸지 않는다.**

> **구현했다고 주장하기 전에 실제 코드와 동작을 확인한다.**

> **기존에 정상 작동하는 기능을 이유 없이 파괴하지 않는다.**

> **문제가 발견되면 숨기지 말고 원인과 해결 방법을 명확하게 기록한다.**

> **사용자의 기존 편집 내용과 템플릿 데이터를 가능한 한 보호한다.**

> **디자인보다 기능, 기능보다 검증 가능한 정확성을 우선한다.**

> **AI는 개발을 돕는 도구이고 최종 판단과 책임은 개발자에게 있다.**

---

# 25. 우리가 지킬 최종 원칙

## “빨리 만드는 것”보다 “확실하게 만드는 것”

코드가 많다고 좋은 결과물이 아니다.

필수 기능 하나라도 정확하게 작동하고,
왜 그렇게 구현했는지 설명할 수 있으며,
실패 상황까지 검증했다면 그것이 더 높은 품질이다.

이번 T03은 단순한 짤 생성기가 아니다.

**기획 → 구현 → 상태 관리 → 파일 처리 → 데이터 관리 → 예외 처리 → 테스트 → 검증 → 배포까지 하나의 작은 제품을 완성하는 과제**라고 생각하고 작업한다.

그리고 마지막까지 다음 질문을 반복한다.

> 이 결과를 내가 직접 만든 프로젝트라고 자신 있게 설명할 수 있는가?

그 답이 **YES**가 될 때까지 확인하고 다듬는다.
