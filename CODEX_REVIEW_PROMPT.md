# 코드 리뷰 요청 — 또 다른 나 (SKT ALEPH T03)

## 당신이 할 일

이 저장소를 **시니어 엔지니어 관점에서 리뷰**해 주세요.

**이번 단계에서는 코드를 고치지 마세요.** 리뷰 보고서만 작성합니다.
무엇을 고칠지는 보고서를 읽고 사람이 결정합니다.

## 무엇을 만드는 중인가

같은 사진과 같은 문구를 **누구에게 보여주느냐**(모습)와
**어느 시대의 인터넷 문법으로 보여주느냐**(시대)에 따라 다르게 표현하는
카드 스튜디오입니다. 브라우저 안에서만 동작하고 서버가 없습니다.

- 공개 주소: https://myeongjundev.github.io/t03-card-studio/
- 저장소: https://github.com/myeongjundev/t03-card-studio
- React 19 + Vite. 런타임 의존성은 `react`, `react-dom` 둘뿐
- 소스 약 5,500줄 (테스트·스크립트 포함)

### 축 두 개

| | 값 | 하는 일 |
|---|---|---|
| 모습(Persona) | 기본 / 소셜 / 친한 친구 | 문구를 **어떻게 받치는가** |
| 시대(Era) | 2004 / 2012 / 2026 | 사진의 **화질과 틀** |

- `2004` 싸이월드 미니홈피 + 그 시절 카메라 화질(뭉개짐·보라 기울기·둥근 검은 테두리)
- `2012` 필름 인화 사진 화질(입자·색바램·비네팅·날짜 각인)
- `2026` 원본 그대로 + 하단 그라데이션

## 먼저 읽어 주세요

1. `docs/ARCHITECTURE.md` — 데이터 흐름, 렌더 순서, 설계 판단과 그 이유
2. `work-log/2026-08-25.md` — 오늘 무엇을 왜 바꿨는지, 실패한 시도 포함
3. `SKT_ALEPH_T03_CHECKLIST.md`, `SKT_ALEPH_T03_WORKING_PRINCIPLES.md` — 과제 요구사항

## 깨뜨리면 안 되는 불변식

리뷰 중 이것이 깨질 수 있는 지점을 찾으면 최우선으로 보고해 주세요.

1. **미리보기 = 내보내기** — 미리보기 Canvas를 그대로 `toBlob()` 한다.
   화면용/저장용 렌더러를 따로 두지 않는다. 다시 그리지 않는다.
2. **JSON은 전체 검증 후에만 저장** — `templates/schema.js`는 저장소에
   접근조차 하지 않는 순수 함수다.
3. **오류가 사용자 데이터를 지우지 않는다** — 손상된 저장소는 지우지 않고
   `.corrupt-<시각>`으로 격리한다.
4. **Canvas 렌더는 동기 실행** — `requestAnimationFrame`은 백그라운드 탭에서
   발화하지 않아 빈 이미지가 저장될 수 있다.
5. **이미지는 어디에도 전송하지 않는다** — 모든 처리가 브라우저 안에서.
6. **렌더는 결정적이어야 한다** — 필름 입자와 저화질 노이즈에 `Math.random`을
   쓰지 않고 씨앗을 고정한 난수를 쓴다. 같은 설정이면 항상 같은 그림.

## 검사 실행 방법

```bash
npm install
npm test                            # 단위 64개, 브라우저 불필요
npx playwright install chromium     # 최초 1회
npm run build && npm run test:e2e   # 실제 브라우저 픽셀 검사 12개
```

배포 워크플로(`.github/workflows/deploy.yml`)가 둘 다 실행하며,
실패하면 배포되지 않습니다. 통과한 테스트 개수가 0이면 실패 처리합니다
(`node --test`는 파일을 못 찾아도 exit 0이라서).

## 특히 봐 주었으면 하는 것

### 1. 렌더러가 너무 커졌는지

`src/render/renderCard.js`가 **845줄**입니다. 배경·시대 장식·사진 화질
보정·모습 받침·문구를 전부 담고 있습니다.

- 쪼개야 하는가? 쪼갠다면 경계를 어디에 두는 것이 옳은가?
- 지금 구조에서 새 시대를 하나 더하려면 몇 군데를 고쳐야 하는가?
- 레이어 순서(`under` → 사진 → `over` → 모습 받침 → 문구)가 지켜지도록
  강제되고 있는가, 아니면 관례에 기대고 있는가?

### 2. 모습 × 시대 조합의 구조

`src/state/presets.js`의 `COMBINATION_STYLES`에 9개 조합이 **하드코딩**되어
있습니다. 축을 하나 더하면 27개가 됩니다.

- 이 방식이 감당 가능한가? 대안이 있다면 무엇을 잃고 무엇을 얻는가?
- 시대가 장식을, 모습이 받침을 맡는 분리가 실제로 지켜지고 있는가?

### 3. 성능

- 사진 화질 보정이 렌더마다 `getImageData`/`putImageData`를 부릅니다
  (300px 버퍼). 문구를 빠르게 타이핑하거나 슬라이더를 끌 때 문제가 되는가?
- 가독성 검사도 렌더마다 픽셀을 읽습니다.
- 캐시할 수 있는데 안 하고 있는 것이 있는가?

### 4. 상태 관리

`src/App.jsx`가 **589줄**입니다. 편집 상태, 히스토리, 이미지 로딩,
공유 링크, 템플릿, 알림을 전부 들고 있습니다.

- 쪼개야 하는가? 어디서부터?
- 되돌리기 묶음(`state/history.js`의 `advanceBurst`)의 ref 관리가 안전한가?
  경쟁 상태가 생길 수 있는 경로가 있는가?

### 5. 테스트가 정말 잡아 주는가

단위 테스트는 진짜 Canvas 없이 **그리기 호출 기록**을 비교합니다
(`test/helpers/traceContext.js`). 픽셀 검사는 헤드리스 Chromium에서 합니다.

- 이 분리가 타당한가?
- **통과하지만 아무것도 지키지 않는 테스트**가 있는가?
- 회귀 스냅샷(`test/fixtures/era-render.json`, 27개 조합)이 너무 촘촘해서
  정상적인 변경마다 깨지지는 않는가?

### 6. 접근성

- 파일 입력을 화면에서 감추고 `<label>`을 드롭존으로 씁니다
  (`src/components/DropZone.jsx`). 키보드·스크린리더에서 정말 쓸 수 있는가?
- 접이식 묶음(`<details>`) 안에 들어간 조작을 스크린리더가 찾을 수 있는가?
- Canvas 결과물을 보조기술 사용자가 알 방법이 있는가?

### 7. 보안·개인정보

과제 체크리스트 9장 요구사항입니다.

- 공개 페이지나 git 이력에 API Key / Token / Password / Secret이 없는가?
- 이미지가 외부로 나가는 경로가 정말 없는가?
- 공유 링크(`src/io/shareLink.js`)가 주소에 담는 값에 문제가 없는가?
- `localStorage` 사용에 위험이 있는가?

## 제가 아는 약한 곳 (여기 없는 것도 찾아 주세요)

정직하게 밝혀 두는 편이 리뷰에 도움이 될 것 같습니다.

- `renderCard.js` 845줄, `App.jsx` 589줄 — 둘 다 크다
- 시대 장식의 문구(`MY HOME`, `COLOR 400`, `TODAY 23 | TOTAL 12,458`,
  `Director. My Life`)가 렌더러에 하드코딩되어 있고 번역/설정이 불가능하다
- `close-friends+2004`처럼 두 줄이 넘는 문구는 다이어리 칸을 벗어날 수 있다
- 데스크톱 왼쪽 패널이 여전히 1,400px 정도로 길다
- 모바일에서 시대별 감성이 작은 화면에서도 읽히는지 확인하지 않았다

## 보고서 형식

```
## 요약
(3~5줄. 지금 상태를 한 문단으로)

## P0 — 지금 고쳐야 함
(불변식이 깨지거나, 사용자 데이터가 사라지거나, 보안 문제)

## P1 — 제출 전 고치는 편이 좋음

## P2 — 나중에

## 좋았던 점
(칭찬이 목적이 아니라, 유지해야 할 판단을 알기 위해)
```

각 항목에 **파일:줄 번호**와 **왜 문제인지**를 적어 주세요.
"이렇게 하는 게 좋다"보다 **"이 조건에서 이렇게 깨진다"**가 유용합니다.

가능하면 **재현 방법**을 함께 적어 주세요. 실제로 깨지는 것을 확인했다면
그렇게 적고, 코드만 읽고 추측한 것이라면 그것도 그렇게 적어 주세요.
둘을 구분해 주시면 판단하기 쉽습니다.
