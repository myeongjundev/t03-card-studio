# Claude 리뷰 요청 — ALTER / EGO: Time-Travel Identity Studio

당신은 인터랙티브 웹 포트폴리오를 심사하는 **시니어 프로덕트 디자이너이자
프런트엔드 엔지니어**다. 이 저장소의 현재 구현을 냉정하게 검토해 달라.

이번 단계에서는 **코드를 수정하지 말고 리뷰 보고서만 작성한다.**
문제를 직접 재현하고 근거를 수집하되, 파일 수정·커밋·배포는 하지 않는다.

## 1. 프로젝트 위치와 현재 기준

- 로컬 경로: `C:\gov\SKT_aleph\t03-card-studio`
- 공개 페이지: https://myeongjundev.github.io/t03-card-studio/
- 저장소: https://github.com/myeongjundev/t03-card-studio
- 기준 브랜치: `main`
- 검토 기준 커밋: `6b1b4a4 feat: brighten the studio with sky and ivory`
- 스택: React 19, Vite 8, Canvas 2D
- 런타임 의존성: `react`, `react-dom`
- 현재 자동 검증: 단위 테스트 70개, Chromium E2E 12개

검토를 시작할 때 `git status`, `git log -3 --oneline`, `package.json`을 확인해
실제 상태가 위 설명과 다른지 먼저 알려 달라.

## 2. 먼저 읽을 문서

다음 순서대로 읽는다.

1. `SKT_ALEPH_T03_WORKING_PRINCIPLES.md`
2. `SKT_ALEPH_T03_CHECKLIST.md`
3. `T03 ALTER - EGO — Award-Winning Web Design Benchmark Directive.md`
4. `T03_ALTER_EGO_TEMPORAL_SCANNER_CODEX_PROMPT.md`
5. `docs/ARCHITECTURE.md`
6. `docs/PORTFOLIO-CASE-STUDY.md`
7. `work-log/HANDOFF-2026-08-25.md`
8. `work-log/2026-08-26.md`

문서의 과거 계획보다 현재 코드와 아래의 최종 제품 방향을 우선한다.

## 3. 제품이 해결하려는 문제

ALTER / EGO는 같은 사진을 2004·2012·2026의 인터넷 문법으로 다시 기록하는
local-first 인터랙티브 카드 스튜디오다.

- 사용자는 첫 화면의 `Temporal Scanner` 경계선을 움직여 두 시대를 한 장면에서 비교한다.
- 선택한 시대는 CTA를 누를 때만 실제 Studio 편집 상태에 반영된다.
- `기본 / 소셜 / 친한 친구` Persona는 누구에게 보이는 나인지 표현한다.
- `2004 / 2012 / 2026` Era는 사진 화질, 프레임, 메타데이터와 기록 문법을 바꾼다.
- 사용자가 올린 이미지는 서버로 전송되지 않는다.
- 결과는 1:1, 4:5, 9:16 PNG로 저장하거나 링크로 설정을 공유할 수 있다.

핵심 메시지는 다음 한 문장이다.

> 같은 사진. 다른 시대. 다른 나.

## 4. 현재 시각 방향

최근 전체 페이지를 어두운 전시형 화면에서 **웜 아이보리 + 하늘색 + 네이비** 기반의
현대적인 제작 도구로 변경했다.

| 역할 | 색상 |
| --- | --- |
| 전체 배경 | `#f5f0e6` |
| 패널 | `#fffdf7` |
| 보조 표면 | `#edf7fb` |
| 제목·본문 | `#183247` |
| 보조 문구 | `#526f80` |
| 주요 CTA | `#1b789f` |

시대 카드 내부에는 2004 아이스 블루, 2012 더스티 핑크, 2026 네온 라임의 개별
표현을 유지했다. 페이지 UI 자체는 아이보리·하늘색·네이비를 중심으로 한다.

이번 리뷰에서 특히 판단해 달라.

1. 밝아진 화면이 개성을 잃지 않으면서 더 현대적이고 사용자 친화적으로 보이는가?
2. Hero의 큰 제목, Scanner, CTA 중 무엇이 먼저 보이며 그 순서가 올바른가?
3. 소개 섹션의 큰 문장과 세 시대 카드가 빠르게 이해되는가?
4. Studio가 작품 소개에서 제작 도구로 자연스럽게 전환되는가?
5. 포트폴리오 심사자가 10초 안에 제품의 차별점과 실제 기능을 이해할 수 있는가?

취향만 말하지 말고 **무엇이 왜 그렇게 보이는지** 타이포 크기, 정렬축, 여백,
명도 대비, 시선 이동과 조작 결과를 근거로 설명한다.

## 5. 절대로 깨뜨리면 안 되는 불변식

다음 중 하나가 깨져 있다면 가장 높은 심각도로 보고한다.

### 미리보기와 다운로드는 같은 픽셀

- 미리보기 Canvas 내부 해상도는 출력 해상도와 같다.
- 화면에는 CSS로만 축소한다.
- 다운로드는 현재 미리보기 Canvas를 그대로 `toBlob()` 한다.
- export 시 별도 Canvas에서 다시 렌더링하지 않는다.

### JSON은 전체 검증 후에만 저장

`파일 읽기 → JSON.parse → 전체 구조·타입·범위 검증 → 전부 성공 → 저장` 순서다.
부분적으로 정상인 항목만 저장하면 안 된다.

### 오류가 사용자 데이터를 지우면 안 된다

- 잘못된 이미지, JSON, localStorage 때문에 기존 편집과 템플릿을 지우지 않는다.
- 손상된 저장소는 삭제하지 않고 별도 corrupt 키로 격리한다.
- Persona/Era 변경 때문에 History를 초기화하지 않는다.

### Canvas 렌더는 동기 실행이며 결정적이다

- 백그라운드 탭에서 멈출 수 있는 `requestAnimationFrame` 렌더로 바꾸지 않는다.
- 필름 입자와 노이즈에 `Math.random()`을 사용하지 않는다.
- 같은 설정을 두 번 렌더하면 같은 PNG가 나와야 한다.

### Hero 탐색과 편집 상태는 분리된다

- Scanner를 움직이는 동안 실제 EditorState를 바꾸지 않는다.
- `ENTER ERA` CTA를 눌렀을 때만 선택 시대와 추천 Persona/비율을 적용한다.
- 사용자가 입력한 문구와 이미지는 유지한다.

### 이미지는 외부로 전송하지 않는다

- 업로드 이미지를 서버, 분석 API, 외부 저장소로 보내지 않는다.
- 공유 링크에는 설정만 담고 이미지 데이터는 담지 않는다.

## 6. 집중 검토 영역

### A. 첫인상과 포트폴리오 경쟁력

- 일반적인 카드 편집기 또는 AI 랜딩페이지처럼 보이는 부분은 어디인가?
- `Temporal Scanner`가 실제 signature interaction으로 느껴지는가, 장식처럼 보이는가?
- 편집적 타이포와 화면 리듬이 모방이 아니라 프로젝트 고유 경험으로 정착했는가?
- 기능 수가 아니라 문제 정의, 디자인 판단, 구현 깊이가 화면에서 드러나는가?
- 채용 담당자에게 보여줄 포트폴리오로 넣을 수준인지 냉정하게 판정하라.

### B. 정보 구조와 사용자 흐름

- Hero → Privacy → Why These Eras → Three Identities → Studio 순서가 자연스러운가?
- 첫 방문자가 `바로 짤 만들기`와 시대 탐색 중 무엇을 해야 할지 혼란스럽지 않은가?
- Scanner의 드래그 가능성, 현재 시대, 스냅 결과와 CTA 결과가 명확한가?
- 모바일 CTA가 실제 미리보기로 바로 이동하는가?
- 30초 빠른 시작의 `모습 → 시대 → 내 사진` 순서가 실제 편집 패널과 일치하는가?
- 긴 Studio에서 사용자가 현재 결과와 다음 행동을 잃지 않는가?

### C. 반응형·접근성

- 375×812, 390×844, 768×1024, 1100px 전후, 1440×900에서 확인한다.
- 가로 넘침, 제목의 부자연스러운 줄바꿈, 잘린 컨트롤과 과도한 빈 공간을 찾는다.
- 키보드만으로 Scanner, Persona, Era, 업로드, 편집, 저장을 사용할 수 있는가?
- `aria-valuenow`, `aria-pressed`, 상태 알림, Canvas 대체 설명이 실제 상태와 맞는가?
- 포커스 표시가 밝은 배경과 어두운 카드 양쪽에서 보이는가?
- 보조 문구, CTA, disabled 상태, 입력 placeholder의 대비가 충분한가?
- `prefers-reduced-motion`에서 핵심 정보가 사라지지 않는가?

### D. Canvas와 편집 정확성

- Persona/Era 변경이 문구·이미지·사용자 비율을 의도치 않게 덮어쓰는가?
- 27개 Persona × Era × Ratio 조합에서 이미지 영역이 Canvas 밖으로 나가는가?
- 투명 배경에서 시대 장식이 불투명 픽셀을 남기는가?
- 긴 한글, 강제 개행, 빈 문구, 이모지, 극단 글자 크기가 안전한가?
- 2004/2012 이미지 보정 캐시가 잘못된 이미지나 옵션에 재사용되는가?
- Canvas 미리보기, 이미지 복사, PNG 다운로드의 결과가 일치하는가?

### E. 상태·저장·복구

- 연속 타이핑 burst와 Persona/Era 변경이 Undo 단계에서 올바르게 분리되는가?
- Undo → 새 편집 후 Redo 가지가 안전하게 제거되는가?
- 템플릿 저장·수정·불러오기·삭제와 JSON 왕복이 정상인가?
- legacy `professional`/`chaotic` 값만 `normal`로 이관되고 임의 값은 거부되는가?
- 손상된 localStorage 격리 실패 시 원본이 후속 저장으로 덮이지 않는가?
- 공유 링크가 기존 `#card=` 상태를 잃지 않는가?

### F. 성능과 자산

- Hero와 Archive의 AVIF 두 장이 실제 빌드에도 52KB·85.57KB 수준으로 포함되는가?
- 원본 PNG가 앱 번들에 중복 포함되지 않는가?
- Scanner 드래그, 긴 문장 입력, 슬라이더 이동에서 체감 프레임 저하가 있는가?
- 동기 Canvas와 동일 Canvas 다운로드 불변식을 유지하면서 개선할 병목만 제안한다.
- 외부 Google Fonts가 늦거나 실패했을 때 레이아웃 이동과 기능 차단이 있는가?

### G. 코드 구조와 유지보수성

- `src/App.jsx`, `src/render/renderCard.js`, `src/styles.css`의 책임이 과도하게 커졌는가?
- 시대 정의의 단일 출처인 `src/state/eras.js`와 다른 하드코딩이 어긋나는가?
- Scanner와 EditorPanel의 시대 메타데이터가 중복되어 drift할 수 있는가?
- CSS 뒤쪽 override가 앞쪽 규칙을 덮는 구조가 수정 위험을 키우는가?
- 새 Persona 또는 Era를 추가할 때 수정 지점과 테스트 비용이 과도한가?
- 죽은 코드, 사용되지 않는 스타일, 오래된 어두운 팔레트가 남아 있는가?

### H. 문서와 제출 준비

- README, ARCHITECTURE, PORTFOLIO-CASE-STUDY, work-log가 현재 밝은 디자인과 실제 기능을 설명하는가?
- README 대표 이미지가 현재 UI를 오해하게 만들지는 않는가?
- 포트폴리오 사례 설명이 기능 나열보다 문제·판단·검증을 보여주는가?
- 공개 페이지의 title, description, Open Graph 정보가 충분한가?
- 코드와 Git 이력에 API Key, Token, Password, Secret 또는 개인정보가 없는가?

## 7. 반드시 실행할 검증

PowerShell에서는 실행 정책 문제를 피하려고 `npm` 대신 `npm.cmd`를 사용한다.

```powershell
npm.cmd test
npm.cmd run build
npm.cmd run test:e2e
git diff --check
```

테스트가 통과했다는 문서의 주장만 믿지 말고 직접 실행한다. 테스트 개수와 실패 여부,
빌드 산출물의 주요 크기를 보고서에 기록한다.

## 8. 실제 브라우저 검증 시나리오

가능하면 로컬 앱과 공개 페이지를 모두 확인한다. 코드 추론과 실제 재현을 구분한다.

1. 첫 화면에서 2004 → 2012 → 2026을 포인터로 드래그한다.
2. Scanner를 방향키, Home, End로 조작한다.
3. 중간 드래그 상태에서 두 시대가 경계 양쪽에 실제로 동시에 보이는지 확인한다.
4. 2004 CTA를 누르고 Studio의 선택 시대와 비율이 반영되는지 확인한다.
5. 사진 한 장을 불러오고 문구를 `오늘도 무사히 😀`로 바꾼다.
6. 기본+2026, 소셜+2012, 친한 친구+2004를 차례로 확인한다.
7. 비율 1:1, 4:5, 9:16을 바꾸고 문구를 Canvas에서 직접 이동한다.
8. Undo/Redo, 템플릿 저장·불러오기, 공유 링크, 이미지 복사, PNG 다운로드를 확인한다.
9. 잘못된 JSON과 잘못된 이미지 입력 후 기존 상태가 보존되는지 확인한다.
10. 작품으로 보기와 모바일 플로팅 미리보기를 확인한다.

## 9. 보고서 형식

발견 사항을 칭찬이나 요약보다 먼저, 심각도 순으로 쓴다.

각 finding은 다음 형식을 지킨다.

```text
### [P0/P1/P2/P3] 짧은 제목
- 위치: 파일:줄번호 또는 화면/뷰포트
- 문제: 한 문장
- 재현: 구체적인 입력과 조작 순서
- 근거: 코드 추론 / 자동 테스트 / 실제 브라우저
- 영향: 사용자가 실제로 겪는 결과
- 제안: 불변식을 깨지 않는 최소 수정
- 수정 위험도: 낮음 / 중간 / 높음
```

심각도 기준:

- `P0`: 보안·개인정보·데이터 손실 또는 핵심 불변식 위반
- `P1`: 주요 기능 실패, 제출을 막는 접근성·모바일·정확성 문제
- `P2`: 포트폴리오 품질이나 유지보수성을 눈에 띄게 낮추는 문제
- `P3`: 작은 마감, 문구, 일관성 개선

마지막에는 다음을 별도 작성한다.

1. **최종 판정** — 지금 포트폴리오에 넣을 수준인지 `YES / CONDITIONAL YES / NO`
2. **10초 첫인상** — 무엇을 만드는지, 무엇이 차별점인지 즉시 이해되는지
3. **출시·제출을 막는 문제**
4. **제출 전 고치면 효과가 큰 세 가지**
5. **현재 그대로 유지해야 할 설계 판단**
6. **부족한 테스트 우선순위**
7. **실제로 실행한 명령과 결과**
8. **확인하지 못한 범위와 이유**

문제가 없는 영역은 단순히 `문제 없음`이라고 쓰지 않는다. 무엇을 어떤 방법으로
확인했으며 그 범위에서는 발견하지 못했다고 적는다.

다시 강조한다. **코드를 수정하거나 커밋하지 말고 리뷰 보고서만 작성하라.**
