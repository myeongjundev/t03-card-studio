# Claude 코드 리뷰 요청 — T03 카드 스튜디오 / 또 다른 나

너는 이미 구현된 React 프로젝트를 검토하는 시니어 코드 리뷰어다.

**코드를 바로 수정하지 마라.** 먼저 문서, 현재 코드, Git diff, 테스트를 확인한 뒤
문제를 심각도 순으로 보고하라. 내가 수정할 항목을 고른 뒤에만 코드를 변경한다.

## 프로젝트

- 경로: `C:\gov\SKT_aleph\t03-card-studio`
- 스택: React 19 + Vite 8
- 런타임 의존성: React와 React DOM뿐
- 기준 커밋: `b41c7a9 feat: add undo/redo with time-based burst coalescing`
- 현재 변경사항: 아직 커밋하지 않은 working tree에 존재

먼저 반드시 다음 문서를 읽어라.

1. `SKT_ALEPH_T03_WORKING_PRINCIPLES.md`
2. `SKT_ALEPH_T03_CHECKLIST.md`
3. `docs/ARCHITECTURE.md`
4. `ALTER EGO · T03 구현 에이전트 프롬프트.md`

그다음 `git diff`, 관련 소스와 테스트를 읽어라. 문서의 과거 계획보다 **현재 사용자가
최종 결정한 아래 제품 방향**을 우선한다.

## 현재 제품 방향

기존 카드 편집기를 한국적인 온라인 기록 감성의 제품 **“또 다른 나”**로 개편했다.

화면에서 선택하는 모습은 세 가지다.

- `기본`: 장식 없는 바닐라 카드. 기본값은 1:1, 전체 배경, 중앙 문구다.
- `소셜`: 공유와 주목을 위한 피드·숏폼 스타일이다.
- `친한 친구`: 비공개 기록과 2000년대 미니홈피 다이어리 감성이다.

초기에 있던 `PROFESSIONAL`과 `CHAOTIC`은 최종 제품에서 제거했다.
다만 이전 JSON/공유 링크의 `professional`과 `chaotic` 값은 사용자 데이터를
버리지 않고 `normal`로 이관해야 한다.

시대 선택은 세 가지다.

- 2004: 개인 홈·메신저
- 2012: 프로필·피드
- 2026: 숏폼·현재형 표현

Persona와 Era 조합은 색상만 바꾸는 것이 아니라 비율, 이미지 영역, 프레임,
메타데이터, 문구 위치와 계층을 바꾼다. 사용자가 입력한 실제 문구와 이미지는
바꾸거나 삭제하지 않는다.

## 절대로 깨뜨리면 안 되는 불변식

### 1. 미리보기와 다운로드는 같은 픽셀

- 미리보기 Canvas 내부 해상도는 출력 해상도와 동일하다.
- 화면에는 CSS로만 축소한다.
- 다운로드는 현재 미리보기 Canvas를 그대로 `toBlob()` 한다.
- export 시 다시 렌더링하거나 별도 Canvas를 만들면 안 된다.
- 이 원칙을 위반하던 `src/io/exportAllRatios.js`는 이번 변경에서 제거했다.

### 2. JSON은 전체 검증 후에만 저장

`파일 읽기 → JSON.parse → 전체 구조·타입·범위 검증 → 전부 성공 → 저장` 순서다.

- `src/templates/schema.js`는 localStorage와 React 상태에 접근하지 않는 순수 함수다.
- 부분 가져오기를 허용하지 않는다.
- schemaVersion은 계속 1이며 `persona`와 `era`는 선택 필드다.

### 3. 오류가 사용자 데이터를 지우면 안 됨

- 잘못된 이미지, JSON, localStorage 때문에 현재 편집이나 기존 템플릿을 지우지 않는다.
- 손상된 저장소는 삭제하지 않고 별도 corrupt 키로 격리한다.
- Persona/Era 변경 때문에 History를 초기화하지 않는다.

### 4. Canvas 렌더는 동기 실행

백그라운드 탭에서 rAF가 멈춰 빈 Canvas가 저장됐던 문제가 있었으므로, 별도 해결책
없이 `requestAnimationFrame` 기반 렌더로 되돌리지 않는다.

## 이번 변경의 핵심 파일

- `src/state/presets.js`: 세 가지 모습, 2004/2012/2026, 조합 스타일
- `src/state/editorState.js`: `persona`, `era` 선택 상태와 기본 바닐라 카드
- `src/render/composition.js`: 조합별 이미지 영역 계산
- `src/render/renderCard.js`: 동일 Canvas 안의 프레임과 메타데이터 렌더
- `src/state/readyCheck.js`: 기존 layout/contrast 결과를 조합한 게시 전 확인
- `src/components/EditorPanel.jsx`: 모습 및 시대 선택 UI
- `src/components/PreviewPanel.jsx`: 게시 전 확인과 9:16 안전 영역 DOM 가이드
- `src/templates/schema.js`: 선택 필드 검증과 legacy 값 이관
- `src/io/shareLink.js`: Persona/Era 공유 링크 왕복
- `src/styles.css`: 한국형 기록장 분위기의 UI

## 현재 자동 검증

- Node 테스트: 48개 통과
- Vite production build: 성공

통과했다는 주장만 믿지 말고 직접 명령을 실행하라. 패키지 스크립트는
`package.json`에서 확인한다.

## 집중 검토 항목

### A. 정확성 및 불변식

- 다운로드가 실제로 현재 미리보기 Canvas만 사용하는가?
- 제거한 일괄 export 경로가 다른 곳에 남아 있지 않은가?
- Persona/Era 전환이 문구·이미지를 보존하는가?
- 조합별 imageBox가 모든 비율에서 Canvas 밖으로 나가지 않는가?
- 프레임을 이미지보다 앞/뒤에 그리는 순서가 의도와 맞는가?
- 투명 배경에서 시대 프레임이 의도치 않게 불투명 픽셀을 만드는가?
- 장식용 Canvas 텍스트가 사용자 문구의 font/textAlign/textBaseline 상태를 오염시키지 않는가?
- 2004/2012/2026 조합에서 긴 문장·이모지·빈 문구가 깨지지 않는가?

### B. 상태와 History

- Persona 적용과 Era 적용이 각각 atomic update인가?
- 직전 타이핑 burst와 합쳐지지 않고 독립 Undo 단계가 되는가?
- Persona → Era → 수동 편집 → Undo/Redo 순서에서 기대 상태로 복원되는가?
- 이전 `professional`/`chaotic` 상태가 `normal`로 이관되는 경로에 누락이 없는가?

### C. Template / JSON / 공유 링크

- 예전 JSON에 persona/era가 없어도 정상 복원되는가?
- 존재하는 persona/era가 잘못된 타입 또는 값이면 전체 가져오기를 거부하는가?
- legacy 값 이관이 검증을 약하게 만들어 임의 값을 통과시키지 않는가?
- 템플릿과 공유 링크에서 조합이 동일하게 왕복되는가?
- schemaVersion 1 유지가 실제 하위 호환 범위에서 타당한가?

### D. READY CHECK / Safe Area

- 새 분석 엔진을 중복 구현하지 않고 기존 contrast/layout을 재사용하는가?
- 9:16 가이드는 DOM 오버레이여서 PNG에 포함될 수 없는가?
- 안전 영역 경고의 경계값과 표현이 과장되거나 오해를 만들지 않는가?
- 빈 문구 또는 폰트 로딩 전 상태를 잘못 READY로 표시하지 않는가?

### E. UI/접근성/한국어 UX

- `기본 / 소셜 / 친한 친구`의 차이가 선택 전에도 이해되는가?
- `기본`이 실제로 장식 없는 바닐라 모드인가?
- 2004 미니홈피 감성이 특정 서비스를 그대로 복제하지 않고 재해석됐는가?
- 버튼의 `aria-pressed`, 그룹 이름, 상태 알림이 적절한가?
- 키보드만으로 모든 주요 기능을 사용할 수 있는가?
- 390px 모바일과 1100px 전환 구간에서 가로 넘침이나 조작 불편이 없는가?
- 한글화되지 않은 사용자 노출 문자열이나 어색한 번역이 남아 있지 않은가?
- 따뜻한 한국형 기록장 디자인이 가독성과 명도 대비를 해치지 않는가?

### F. 성능

- 타이핑마다 최대 1080×1920 Canvas 렌더와 픽셀 대비 검사가 동기로 실행된다.
- 시대 장식과 composition 추가가 체감 지연을 키웠는지 실제 측정하라.
- 측정 없이 느리다고 단정하지 마라.
- 개선안은 동기 렌더와 동일 Canvas 다운로드 불변식을 반드시 유지해야 한다.

### G. 테스트 부재와 죽은 코드

- 새 조합의 실제 Canvas 렌더 순서와 픽셀 결과를 검증하는 테스트가 부족하지 않은가?
- legacy migration, History boundary, 투명 PNG, Safe Area에 필요한 회귀 테스트가 있는가?
- 제거된 네 번째 Persona와 일괄 export의 CSS·문서·import·dead branch가 남아 있는가?
- README, ARCHITECTURE, VERIFICATION이 실제 동작과 일치하는가?

## 실제 브라우저 검증 시나리오

가능하면 로컬 앱을 실행해 다음을 직접 확인하라. 코드만 보고 추론한 것과 브라우저에서
재현한 것을 명확히 구분한다.

1. 문구를 `오늘도 무사히 😀`로 바꾼다.
2. 이미지를 한 장 불러온다.
3. `기본 + 2026`에서 1:1 바닐라 카드인지 본다.
4. `친한 친구 + 2004`에서 프레임·날짜·카운터·다이어리 구획이 생기는지 본다.
5. `소셜 + 2026`에서 9:16과 안전 영역이 동작하는지 본다.
6. 각 전환을 Undo/Redo한다.
7. 템플릿 저장/불러오기와 JSON 왕복을 확인한다.
8. 다운로드 PNG와 다운로드 직전 Canvas를 가능하면 픽셀 비교한다.
9. 잘못된 JSON과 잘못된 이미지를 넣고 기존 상태가 보존되는지 확인한다.

## 보고 형식

발견 사항을 심각도 순으로 정렬한다. 단순 요약보다 actionable finding을 먼저 쓴다.

각 항목은 반드시 다음 형식을 따른다.

- 심각도: P0 / P1 / P2 / P3
- 위치: `파일:줄번호`
- 문제: 한 문장
- 재현 조건: 구체적인 입력과 조작 순서
- 근거: 코드 추론 / 자동 테스트 / 실제 브라우저 중 무엇인지
- 영향: 사용자가 실제로 겪는 결과
- 제안: 위 불변식을 깨지 않는 최소 수정
- 수정 위험도: 낮음 / 중간 / 높음

문제를 찾지 못한 영역은 단순히 “문제 없음”이라고 쓰지 말고, 무엇을 어떤 방식으로
확인했고 그 범위에서는 발견하지 못했다고 적는다.

마지막에는 별도로 다음을 정리한다.

1. 출시/제출을 막는 문제
2. 제출 전 고치면 좋은 문제
3. 현재 그대로 유지해야 하는 설계
4. 부족한 테스트 우선순위
5. 실제 실행한 명령과 결과

다시 강조한다. **이번 단계에서는 코드를 수정하지 말고 리뷰 보고서만 작성하라.**
